openapi: 3.1.0

info:
  title: Propel MBOM API
  version: 1.0.0
  summary: Create, read, edit, baseline and reconcile a Manufacturing BOM.
  description: |
    The complete REST surface of Propel's Manufacturing BOM (MBOM), built on the new
    **branch / baseline / configuration-item** framework. Eight endpoints cover the whole
    lifecycle: create an MBOM over a released eBOM revision, read its structure, edit it with
    delta operations, freeze it as a baseline, and reconcile it against a newer eBOM revision.

    ---

    ## Read this first: the eight ideas the whole API rests on

    ### 1. An MBOM inherits its structure. It never copies it.

    Creating an MBOM writes **four records and not one row of BOM structure**. The branch stores
    only its own *deltas*; everything else is resolved at read time by walking the live legacy
    `Assembly__c` graph beneath the accepted eBOM root revision. An untouched branch holds zero
    per-edge records however many times it is read.

    ### 2. `T_R` — the pinned source clock

    When an MBOM is created (or a pull is committed), the accepted root revision's
    **release date** is stamped once as `Source_TimePoint__c`. That timestamp is **T_R**, and every
    inherited row resolves against it: a child item renders at whichever revision was released as
    of T_R.

    T_R is *stamped, then read* — never re-derived. So when engineering releases `Assy-100` rev-2,
    **nothing happens to the MBOM**. It still resolves against rev-1's T_R until someone explicitly
    accepts the newer revision with a [pull](#tag/Pull).

    ### 3. `asOf` — the branch clock, and why there are two

    Every read also takes an `asOf` timestamp (defaulting to now). That one governs the *branch's*
    own content — its deltas, its locally added parts, its substitutes.

    Two clocks are therefore legitimately in play at once, and each row states which one governs it
    (`TreeRowDTO.governingTimestamp`, `MemberState.governingClock`):

    | Row kind | Governing clock |
    |---|---|
    | `inherited` — supplied by the source, untouched | **T_R** (`source`) |
    | `claimed` — the branch took copy-on-write ownership | **T_R** for the child revision it pins |
    | `local` — branch-owned, no upstream provenance | **`asOf`** (`branch`) |
    | a *substitute* (from `replace`) | **`asOf`** — its child floats |

    A direct consequence is that **one item can render at two different revisions in the same
    tree**. That is expected, not a fault, and is reported as a `mixed-revision` warning beside the
    data rather than an error instead of it.

    ### 4. Provenance: `inherited` / `claimed` / `local`

    Derived at read time, never stored. `claimed` and `local` differ by exactly one field on the
    winning delta row (`Overrides_Source__c` set vs. null), and that difference is what makes
    "what did I change relative to upstream?" answerable.

    ### 5. Copy-on-write: you must `claim` before you can edit

    An inherited row is read-only. Editing it is refused with `CLAIM_REQUIRED`, carrying the eBOM
    line id so the client can offer the claim. Divergence from upstream is always an explicit act.

    ### 6. Nothing is ever deleted

    Removal is an **exclude delta** — a tombstone appended to an append-only log. `restore` lifts
    it. A hard delete of any branch-owned row is refused (`HARD_DELETE_BLOCKED`). This is what makes
    a read at a past `asOf`, and therefore a baseline, reproducible.

    ### 7. A baseline stores a timepoint, not rows

    Cutting a baseline records one timestamp and freezes the revisions it resolved to. Reading it
    **re-resolves** the branch at that timestamp. Two stored counts act as canaries if something the
    resolution depends on has moved since — `BASELINE_COUNT_DRIFT` for the row count, and
    `BASELINE_PROVENANCE_DRIFT` for how many of those rows are the branch's own overrides.

    ### 8. "Pull", not "merge"

    A pull is the **downstream-initiated acceptance of a newer upstream source version**, matching
    `Branch_Source__c.Source_Type__c`. The word *merge* is deliberately reserved for the Phase 2
    branch-to-branch operation, which is bidirectional and common-ancestor-based — genuinely
    different.

    ---

    ## Response and error conventions

    These conventions differ from other Propel REST endpoints:

    - Success and error bodies are **plain JSON, never wrapped** in an
      `entity` / `entities` envelope.
    - **Error bodies key the code as `code`, not `errorCode`.** `errorCode` appears in this API only
      *inside* a delta batch's per-operation `results[]`.
    - **Nulls are not suppressed.** A field left null for that case is meaningfully null — see
      `DeltaResult.forked`, where `null` and `false` say different things.
    - **The code is the contract; the message is not.** Messages are translated. Switch on `code`,
      never on text.

    ### Status-code scheme

    | Status | Meaning |
    |---|---|
    | **400** | The request could not be read, or a value is out of range. Fix the request. |
    | **403** | No permission. The body carries the label only — never the object that was denied. |
    | **404** | The path, or an id in the body, names nothing. |
    | **409** | Well formed, and **retryable after changing state** — claim it first, restore it first, re-preview. |
    | **413** | More than 200 operations in one delta batch. |
    | **422** | Well formed, but **impossible against the model**. Retrying unchanged will never succeed (a cycle is still a cycle). |
    | **500** | Unmodelled server failure. Carries the exception type and message, never the stack trace. |

    The 409/422 split is deliberate: a client that retries on a generic 4xx would spin forever on a
    cycle, and the two cases want different UI.

    ---

    ## Design intent: MBOM-specific paths, generic vocabulary

    MBOM is the first application of a general branch / baseline / configuration-item framework. The
    path is namespaced under `/mbom` so MBOM-only rules have somewhere to live, but the nouns below
    that segment — **branch**, **source**, **member**, **override**, **baseline** — are framework
    concepts, not MBOM concepts. Everything MBOM-specific is confined to a single discriminated
    schema, `AssemblyEdgeDetail`, reached through `MemberState.detail` and keyed by `ciKind`. A
    second configuration-item type adds a sibling schema there and touches nothing else.

    ## Paths

    There is **no `branchId` path segment on the source endpoints**: a source already names its
    owning branch. `branchId` still appears in the pull-preview *body*, denormalised for display.

  contact:
    name: Propel PLM Engineering
  license:
    name: Proprietary — Propel Software Solutions, Inc.
    identifier: LicenseRef-Propel-Proprietary
  x-epic: https://propelplm.atlassian.net/browse/MVP-30018
  x-jira:
    create: MVP-30027
    deltas: MVP-30028
    trees: MVP-30029
    baselineCut: MVP-30030
    baselineRead: MVP-30031
    pullPreview: MVP-30076
    pullCommit: MVP-30077
    comparison: MVP-30296
  x-design-doc: docs/architecture/mbom/mbom-pull-api.md
  x-demo-plan: docs/architecture/mbom/mbom-demo-test-plan.md

servers:
  - url: https://{instance}.my.salesforce.com/services/apexrest/{namespace}api/v4
    description: Salesforce org
    variables:
      instance:
        default: my-domain
        description: My Domain host for the target org.
      namespace:
        default: PDLM/
        enum:
          - PDLM/
          - ksplm/
          - ''
        description: |
          The managed-package namespace prefixing the REST path. `PDLM/` for the MBOM/BOM package,
          giving `/services/apexrest/PDLM/api/v4/mbom/...`. `ksplm/` for the core PLM package.
          Empty only for a genuinely unmanaged deployment.

          `GET {base}/v4` returns `"v4 live"` when the base URL and namespace resolve.

security:
  - sessionToken: []

tags:
  - name: Lifecycle
    description: Creating an MBOM over a released eBOM revision.
  - name: Structure
    description: |
      Reading resolved structure — the branch's effective content, or what a source supplies on its
      own before any branch deltas are applied.
  - name: Editing
    description: |
      Changing a branch through delta operations. Every change is an append to a log; nothing is
      mutated in place and nothing is deleted.
  - name: Baselines
    description: |
      Freezing and replaying a configuration. A baseline stores a timepoint and re-resolves on read.
  - name: Pull
    description: |
      Reconciling an MBOM branch against a newer version of the eBOM source it inherits from —
      a two-step preview/commit flow.
  - name: Comparison
    description: |
      Answering "how do these two configurations differ?" for any pair drawn from branch, baseline,
      source and released eBOM revision. Read-only, symmetric, and decides nothing.

paths:

  # ==================================================================== lifecycle

  /mbom/mboms:
    post:
      tags: [Lifecycle]
      operationId: createMbom
      summary: Create an MBOM over a released eBOM revision
      description: |
        Creates an MBOM header, its first branch, the branch's eBOM source lineage, and that
        source's first acceptance — **four records in one transaction, and zero rows of BOM
        structure**.

        The response returns all four ids rather than just `mbomId`, because a caller that had to go
        looking for the other three would be querying schema this API exists to hide. The one you
        will use most is `sourceId` — it is the `sourceId` every `/sources/...` endpoint
        takes.

        ## The root revision must be Released

        Its `Released_Date_Time__c` becomes **T_R**, the timepoint every inherited row is pinned at.
        An unreleased revision has no such date, so there is nothing to pin to.

        Four shapes are refused with `NOT_RELEASED` (409, not 400 — releasing the revision makes the
        identical request succeed): not released; **Cancelled**; **Expired**; released but carrying
        no release date. Cancelled and Expired are excluded because the eBOM's own as-of read would
        never resolve them either.

        ## Timestamps stamped here

        | Field | Value | Why it matters |
        |---|---|---|
        | `Source_TimePoint__c` (**T_R**) | the root revision's release date | what inherited rows resolve at, forever |
        | `Accepted_At__c` | the **wall clock**, now | which acceptance governs a given `asOf` |

        A consequence worth knowing before you write a test: because `Accepted_At__c` is the wall
        clock, a branch created now renders **nothing** when read at an `asOf` in the past. That is
        the acceptance not existing yet at that timestamp, not a defect.

        ## Access

        Gated **before the body is read**: a caller with no create grant gets 403 whatever they sent.
        Requires create on `MBOM__c`, `Branch__c`, `Branch_Source__c` and
        `Branch_Source_Rev__c`, plus read on `Item_Revision__c`. All five are named so a partial
        grant is a clean 403 rather than a 500 out of the middle of the DML.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MbomCreateRequest'
            examples:
              standard:
                summary: An MBOM over Assy-100 rev-1
                value:
                  rootItemRevisionId: a0Ag8000000Ab01EAC
                  mbomName: Assy-100 Manufacturing BOM
                  branchLabel: MBOM-GDL
      responses:
        '201':
          description: |
            Created. Four records now exist. 201 rather than 200 because the response names records
            that did not exist before the call, and a client distinguishing "created" from "already
            there" has nothing else to read.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MbomCreateResult'
              examples:
                created:
                  value:
                    mbomId: a1Ag8000000AbCcEAK
                    branchId: a1Bg8000000AbCdEAK
                    sourceId: a1Cg8000000AbCeEAK
                    sourceRevisionId: a1Dg8000000AbCfEAK
        '400':
          description: |
            `INVALID_REQUEST` — the body was empty or unreadable; `mbomName` or `branchLabel` was
            blank; or either exceeded its field length (`mbomName` ≤ 80, `branchLabel` ≤ 80 — the
            shorter of `Branch__c.Name` at 80 and `Label__c` at 255).

            A malformed id in the **body** is a 400 while one in the **URI** is a 404, and that is
            not an inconsistency: the URI is what *names* the resource, so a path naming nothing is
            a path to nothing. A body field is a value in a request to a resource that does exist.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                blankName:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (mbomName is required)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: |
            `NOT_FOUND` — `rootItemRevisionId` was null, is not an `Item_Revision__c` id, or names
            no visible row. The three are not distinguished on purpose: separating "malformed" from
            "absent" would tell an unauthorised caller which ids exist.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                notARevision:
                  value:
                    code: NOT_FOUND
                    message: 'That record does not exist, or you do not have access to it. (a0Bg8000000Ab10EAC is not an Item_Revision__c)'
        '409':
          description: |
            `NOT_RELEASED` — the root revision is unreleased, Cancelled, Expired, or has no release
            date, so there is no T_R to stamp.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                notReleased:
                  value:
                    code: NOT_RELEASED
                    message: 'An mBOM can only be created from a released item revision. (a0Ag8000000Ab99EAC)'
        '500': { $ref: '#/components/responses/InternalError' }

  # ==================================================================== structure reads

  /mbom/branches/{branchId}/tree:
    get:
      tags: [Structure]
      operationId: getBranchTree
      summary: Read a branch's effective structure
      description: |
        The branch's resolved content at one timepoint: the eBOM traversed at **T_R**, with the
        branch's own deltas folded and overlaid at **`asOf`**.

        This is the main read. It is where the two-clock model becomes visible — every row states
        its own `provenance` and its own `governingTimestamp`, because a row that did not carry its
        own clock would be unexplainable to the person looking at it.

        ## What the response contains that you might not expect

        - **Excluded rows are rendered**, carrying `isExcluded: true`. A tree read shows the person
          editing what was removed. (A *baseline* read filters them out — see that endpoint.)
        - **`isEditable: false`** on any row inside the subtree of a locally added or substituted
          assembly. Only content the branch's *source* supplied can be claimed or changed. It is
          stated rather than left to be derived from `provenance`, so a client does not have to
          encode which provenance values happen to be editable this release. Every delta op refuses
          these rows, so a UI offering an action on one is offering an action the API will reject.
        - **Unreachable rows are dropped.** A delta can name a parent item that is not in the tree —
          a subassembly whose item had nothing released at T_R drops out, and a claim beneath it
          survives the overlay as a row with an absent parent. This read walks down from the root and
          keeps only what it can reach, in **both** shapes.
        - **`warnings` and `rows` are always present**, empty rather than absent. A client that has
          to distinguish "no warnings" from "the key is missing" writes null checks at every call
          site, and one of them is eventually forgotten.

        ## Flat vs. nested, and a real trap

        `flat=true` (the **default**) returns one row per edge, each carrying `detail.parentItemId` and an
        empty `childRows` — losslessly re-nestable by the client. `flat=false` nests via `childRows`.

        **The nested shape duplicates a shared subassembly; the flat shape does not.** The traversal
        walks a DAG and emits a shared subtree once; nesting re-materialises it under every parent
        that uses it, because that is what a BOM tree looks like to a person expanding it. So the two
        shapes carry the same *content* but not the same row **count** — never treat a nested row
        count as a total — and a deeply shared DAG can hit heap nested while rendering comfortably
        flat.

        ## Reads create nothing

        No DML anywhere on this path. An untouched branch stays at zero rows however many times it
        is read.
      parameters:
        - $ref: '#/components/parameters/BranchId'
        - $ref: '#/components/parameters/AsOf'
        - $ref: '#/components/parameters/Flat'
      responses:
        '200':
          description: The resolved tree.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StructureTree' }
              examples:
                flat:
                  summary: Flat (default) — inherited, claimed, local and excluded rows
                  $ref: '#/components/examples/BranchTreeFlat'
                nested:
                  summary: Nested (`flat=false`)
                  $ref: '#/components/examples/BranchTreeNested'
                mixedRevision:
                  summary: With a `mixed-revision` warning
                  $ref: '#/components/examples/BranchTreeMixedRevision'
        '400':
          description: |
            `ASOF_INVALID` — `asOf` could not be parsed. `ASOF_IN_FUTURE` — `asOf` is later than now;
            there is no configuration at a timepoint that has not happened.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                asOfInvalid:
                  value:
                    code: ASOF_INVALID
                    message: 'The timepoint could not be read. Use an ISO-8601 timestamp, for example 2026-02-01T00:00:00Z. (yesterday)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFoundPath' }
        '422':
          description: |
            `EBOM_CYCLE` — the source eBOM itself contains a cycle, so its structure cannot be
            resolved. Fix the eBOM; retrying is pointless.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '500': { $ref: '#/components/responses/InternalError' }

  /mbom/sources/{sourceId}/tree:
    get:
      tags: [Structure]
      operationId: getSourceTree
      summary: Read what a source supplies, with no branch deltas applied
      description: |
        The "**what would I inherit?**" read: the structure as one source supplies it, with the
        branch's claims, excludes and replaces deliberately **not** overlaid.

        Use it to answer "what did I change relative to upstream?" by diffing this against
        `GET /mbom/branches/{branchId}/tree`. Every row comes back `provenance: inherited`.

        ## Why there is no `flat` parameter

        A source's own structure has no deltas overlaid on it, so there is no nesting decision to
        make. An unexpected `flat` is **ignored rather than rejected**, so one parameter set can be
        sent to both tree reads.
      parameters:
        - $ref: '#/components/parameters/SourceId'
        - $ref: '#/components/parameters/AsOf'
      responses:
        '200':
          description: The source's own structure.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StructureTree' }
              examples:
                source:
                  $ref: '#/components/examples/SourceTree'
        '400':
          description: '`ASOF_INVALID` or `ASOF_IN_FUTURE` — see the branch tree read.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFoundPath' }
        '422':
          description: '`EBOM_CYCLE` — the source eBOM contains a cycle.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '500': { $ref: '#/components/responses/InternalError' }

  # ==================================================================== editing

  /mbom/branches/{branchId}/deltas:
    post:
      tags: [Editing]
      operationId: applyDeltaBatch
      summary: Apply a batch of delta operations to a branch
      description: |
        The single write endpoint for editing a branch. Eight operations — `claim`, `update`, `add`,
        `exclude`, `restore`, `replace`, `revert`, `convert-to-native` — arrive in one array and are
        applied under one envelope.

        **Returns `200`, not `201`**, even though rows are created: the response is a *report* on a
        set of operations, not a pointer to one new resource, and under `atomic: false` some of them
        may have failed.

        ## How a batch executes

        1. **Size check** — more than 200 operations is `BATCH_TOO_LARGE` (413), refused before
           anything is resolved.
        2. **One snapshot.** The branch is resolved **once**, at `asOf`. That is the whole reason a
           batch endpoint exists: twenty operations resolving the branch each would be twenty
           traversals, and the tree could shift underneath the batch halfway through.
        3. **Validate everything, then apply.** Two passes, never interleaved, so *every* fault is
           reported rather than only the first.
        4. **Persist** in a fixed number of DML statements — twenty claims cost the same statements
           as one.

        ### Order is preserved, but order is not sequencing

        Results come back in request order with your `clientRef` echoed and `index` stated. But
        **every operation validates against the one pre-batch snapshot, so a later operation cannot
        see an earlier one's effect.** `[claim(L), update(L)]` in one batch answers `CLAIM_REQUIRED`
        on the update — and under the default `atomic: true` that rejects the whole batch.

        > **Dependent edits are two requests, not two operations.**

        ## `atomic` — the failure contract is yours to choose

        | `atomic` | Behaviour |
        |---|---|
        | `true` (**default** — a null wins the safer contract) | All-or-nothing. Any validation failure throws: the status is the **first failure's** own code (so a batch whose only fault is `CLAIM_REQUIRED` still answers 409, not a generic 400) and the body carries a `failures[]` array with **every** failure. The branch is left exactly as it was. |
        | `false` | Apply what is valid, report the rest. `200` with per-operation `results[]`. |

        `atomic: false` reports per-operation **validation** failures only. It does not fall back to
        per-operation DML: the aggregate is one savepoint and one set of statements, and a failure
        there rolls the batch back and propagates.

        ## `asOf` chooses what a batch *validates* against; it never dates the write

        Every row written is effective **now**, whatever `asOf` says. A past `asOf` means "validate
        against the configuration as it stood then" — optimistic concurrency, not time travel.
        Backdating the write would let a batch win the current fold *and* change what a read at an
        intervening timestamp returned, rewriting history in an append-only log.

        Several operations therefore **refuse a past `asOf` outright** with `INVALID_REQUEST`,
        because they decide from the branch's *current* state: `restore`, `revert`,
        `convert-to-native`, and any `claim` / `add` / `exclude` / `replace` that has to mint a new
        edge identity.

        ## `Idempotency-Key`

        Read and passed through, and **ignored this phase**. It is accepted so a client can start
        sending it now without its first request failing, and a later phase can honour it without a
        breaking change.

        ## The operations

        | `op` | Required fields | What it does |
        |---|---|---|
        | `claim` | `ebomAssemblyId` | Takes one live eBOM line into the branch as a copy-on-write override, so the branch can diverge from it. Copies the values as they stand. Writes 5 records. |
        | `update` | `assemblyCiId` **or** `ebomAssemblyId`; `quantity` and/or `findNumber` | Revises a branch-owned edge. Edits its `CI_Rev__c` in place while Draft; otherwise allocates a new revision, freezes the old one, and returns `forked: true`. Membership is not touched. |
        | `add` | `parentItemId`, `childItemId`, `quantity`, `findNumber` | Adds a part the eBOM never supplied — packaging, fixtures, consumables. Branch-**local**: `Overrides_Source__c` stays null, and the child **floats** at `asOf`. |
        | `exclude` | `ebomAssemblyId` **or** `ciId` | Tombstones a row. **No cascade is written** — the descendant prune is computed at read time, because a descendant still supplied through another path must survive. |
        | `restore` | `ciId` | Lifts an exclusion. Appends `Add` when the branch holds content for the row, or `Revert` when it does not (an inherited row excluded straight from a stub) — appending `Add` blindly would break the next read. |
        | `replace` | `replacedEbomAssemblyId`, `newChildItemId`, `quantity`, `findNumber` | Substitutes a different child for the one an eBOM line supplies, **recording that it is a substitution** (`Replaces__c`), so the pair does not read as an unrelated removal plus an unrelated addition. |
        | `revert` | `ciId` **or** `ebomAssemblyId` | Discards the branch's override so the row falls back to what its source supplies. Nothing on disk is touched — a `Revert` marker is appended and the fold drops it entirely. |
        | `convert-to-native` | `ciId` | Severs a claimed edge's link to the source it overrides: same content, but now branch-owned in its own right (`provenance` flips `claimed` → `local`). Appends an `Add` with `Overrides_Source__c` null. |

        ### Addressing quirks worth knowing

        - `update` accepts `ebomAssemblyId` **and has to**: an inherited row has no edge record, so
          that is the only id a client holds for the row whose update must come back
          `CLAIM_REQUIRED`. Without it the rejection could never be reached.
        - `revert` accepts `ebomAssemblyId` for the mirror reason: a reverted row renders with no
          `CI__c` at all, so a *second* revert — which must answer `NO_OVERRIDE` — cannot be
          addressed any other way.
        - `replace` only works on eBOM-derived rows. A branch-**local** row has a synthetic
          `local-…` edge key and no `Assembly__c` line, so replacing local content is `update`, or
          `exclude` plus `add`.

        ## Access

        Requires **create** on `Assembly_CI__c`, `CI__c`, `Assembly_CI_Rev__c`, `CI_Rev__c` and
        `Branch_Member__c`, **and edit** on `Assembly_CI__c` (upserted) and `CI_Rev__c` (updated when
        an operation freezes a predecessor). Create and edit are not the same permission, and both
        are named so a partial grant is a 403 before any DML.
      parameters:
        - $ref: '#/components/parameters/BranchId'
        - name: Idempotency-Key
          in: header
          required: false
          description: |
            Accepted and **ignored** this phase. Present so a client can start sending it without
            its first request failing.
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeltaBatchRequest'
            examples:
              claimThenEdit:
                summary: Claim two eBOM lines (atomic, the default)
                value:
                  atomic: true
                  operations:
                    - op: claim
                      clientRef: row-motor
                      ebomAssemblyId: a0Bg8000000Ab10EAC
                    - op: claim
                      clientRef: row-bracket
                      ebomAssemblyId: a0Bg8000000Ab11EAC
              update:
                summary: Revise quantity and find number on a claimed edge
                value:
                  operations:
                    - op: update
                      clientRef: qty-fix
                      assemblyCiId: a1Eg8000000AbD1EAK
                      quantity: 4
                      findNumber: 10
              addLocal:
                summary: Add a part the eBOM never supplied
                value:
                  operations:
                    - op: add
                      clientRef: packaging
                      parentItemId: a02g8000000Ab01EAC
                      childItemId: a02g8000000Ab90EAC
                      quantity: 1
                      findNumber: 90
              excludeAndReplace:
                summary: Substitute an alternate part, and drop another line
                value:
                  operations:
                    - op: replace
                      clientRef: alt-motor
                      replacedEbomAssemblyId: a0Bg8000000Ab10EAC
                      newChildItemId: a02g8000000Ab91EAC
                      quantity: 1
                      findNumber: 10
                    - op: exclude
                      clientRef: drop-harness
                      ebomAssemblyId: a0Bg8000000Ab12EAC
              nonAtomic:
                summary: Best-effort — apply what is valid, report the rest
                value:
                  atomic: false
                  operations:
                    - op: claim
                      clientRef: ok
                      ebomAssemblyId: a0Bg8000000Ab11EAC
                    - op: update
                      clientRef: will-fail
                      ebomAssemblyId: a0Bg8000000Ab12EAC
                      quantity: 9
      responses:
        '200':
          description: |
            The batch report. Also returned when `atomic: false` and some operations failed — check
            `failed` and each `results[].success`, not the status code.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeltaBatchResponse' }
              examples:
                allApplied:
                  summary: Two claims applied
                  $ref: '#/components/examples/DeltaBatchApplied'
                partial:
                  summary: '`atomic: false` — one applied, one refused'
                  $ref: '#/components/examples/DeltaBatchPartial'
                forked:
                  summary: An update that had to fork a frozen revision
                  $ref: '#/components/examples/DeltaBatchForked'
        '400':
          description: |
            `INVALID_REQUEST` — the body could not be read; or (under `atomic: true`) an operation
            failed validation with a 400-mapped code: a missing required field, an unknown `op`, or
            an operation that refuses a past `asOf`.

            `ASOF_INVALID` / `ASOF_IN_FUTURE` — the batch's own `asOf`.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                unknownOp:
                  $ref: '#/components/examples/DeltaBatchUnknownOp'
                pastAsOfRefused:
                  value:
                    code: INVALID_REQUEST
                    message: "That request could not be read. Check that the body is valid JSON and that every required value is supplied. (convert-to-native decides from the branch's CURRENT provenance, which a past asOf cannot report; send it without one)"
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFoundPath' }
        '409':
          description: |
            An atomic batch failed validation, and the first failure's code was a 409. The body
            carries **every** failure in `failures[]`, not just the first — a caller fixing them one
            round trip at a time is the behaviour this endpoint exists to avoid.

            | code | Cause | Remedy |
            |---|---|---|
            | `CLAIM_REQUIRED` | a write targeted an inherited row | `claim` it first |
            | `ALREADY_CLAIMED` | `claim` on a row this branch already owns | edit it directly |
            | `ALREADY_EXCLUDED` | `exclude` on a tombstoned row | `restore` it first |
            | `ALREADY_PRESENT` | `add` of a (parent, child) the branch already carries locally; or an op on a **replaced** row | edit the existing row; or act on the substitute |
            | `NO_OVERRIDE` | `restore`/`revert`/`convert-to-native` on a row the branch asserts nothing about | nothing to undo |
            | `NOT_SUPPLIED` | `revert` on a row no source supplies — falling back would make it vanish | `exclude` instead |
            | `STALE_REVISION` | `update` against a revision superseded since your view | reload and retry |
            | `NOT_EDITABLE` | the target is inside the subtree of a locally added or substituted assembly | only source-supplied content is editable |
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                claimRequired:
                  $ref: '#/components/examples/DeltaBatchAtomicFailure'
        '413':
          description: '`BATCH_TOO_LARGE` — more than 200 operations. Split the change.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                tooLarge:
                  value:
                    code: BATCH_TOO_LARGE
                    message: 'A single request can carry at most 200 operations. Split the change into smaller batches. (240 operations, maximum 200)'
        '422':
          description: |
            Impossible against the model.

            | code | Cause |
            |---|---|
            | `CYCLE_DETECTED` | the requested edge would make a child an ancestor of its own parent |
            | `PARENT_NOT_IN_BRANCH` | `add` targeted a parent that is not part of this configuration |
            | `EBOM_CYCLE` | the source eBOM already contains a cycle |
            | `UNSUPPORTED_CI_TYPE` | a delta named a CI whose `Type__c` has no registered adapter |
            | `CI_IDENTITY_UNRESOLVED` | a CI of a registered type carries no payload row, or one with no join key — corruption, never skipped |
            | `CI_IDENTITY_COLLISION` | two `CI__c` rows resolve to one identity, so one of their deltas would be silently discarded |
            | `PROVENANCE_INVALID` | a delta named a source that is not this branch's |
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                cycle:
                  value:
                    code: CYCLE_DETECTED
                    message: 'This change would make the child an ancestor of its own parent, which no BOM can represent. (Sub-200 is already an ancestor of Assy-100)'
        '500': { $ref: '#/components/responses/InternalError' }

  # ==================================================================== baselines

  /mbom/branches/{branchId}/baselines:
    post:
      tags: [Baselines]
      operationId: cutBaseline
      summary: Cut a baseline over a branch
      description: |
        Records the branch's full effective structure at one timepoint — **as that timepoint**.

        ## A baseline stores a timepoint, not rows

        The cut writes one `Baseline__c` carrying `Resolution_TimePoint__c`, and nothing else about
        the content. Reading it back **replays the resolution** at that timestamp, which is exact
        because the resolution is a pure function of it and because nothing can ever land at or
        before a past T (`Accepted_At__c` and `EffectiveDateTime__c` are both stamped with the wall
        clock).

        The flattened member table this replaces cost one record per edge per cut — roughly 36 MB at
        the measured 18,000-edge ceiling.

        ## The freeze is what makes the replay honest

        A Draft `CI_Rev__c` is edited **in place** by `update`. Replaying a timepoint would therefore
        follow an edit made *after* the cut, and the baseline would change without anyone touching
        it. So the cut freezes every revision the snapshot resolved to. From then on, every `update`
        to those rows forks a new revision instead (and comes back `forked: true`).

        ## Two timestamps, and why they are not one

        `resolutionTimePoint` is what the content was resolved at; `takenAt` is the wall-clock moment
        of the cut. Kept apart, a correction to the server clock cannot change what a baseline
        resolves to. (MVP-30030 called both `cutDateTime`; the **request** still does, the response
        does not.)

        ## Excluded rows are not members

        `memberCount` counts what the configuration *contains*, so tombstoned rows are not counted —
        which means it is **not** the number of rows the tree endpoint renders. The baseline read
        filters on the same predicate, so the stored count stays comparable to a later read.

        ## Immutable from the insert

        There is no draft-baseline state and no lock step: every update to a `Baseline__c` is refused
        outright, and every hard delete too.

        ## Access

        Requires **create** on `Baseline__c`, and **edit** on `Baseline__c` and `CI_Rev__c` (the
        freeze). Named separately because a partial grant would otherwise fail half way through the
        cut — after the members resolve and before the freeze, the one state a baseline must never
        be left in.
      parameters:
        - $ref: '#/components/parameters/BranchId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BaselineCutRequest'
            examples:
              now:
                summary: Cut at the server clock
                value:
                  label: Pilot build 1
              backdated:
                summary: Cut as the configuration stood at an earlier moment
                value:
                  label: Pre-ECO snapshot
                  cutDateTime: '2026-03-01T00:00:00Z'
      responses:
        '201':
          description: |
            The baseline's header. 201 rather than the 200 the delta batch answers: this call names
            one new resource, where a batch reports on a set of operations.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Baseline' }
              examples:
                cut:
                  value:
                    baselineId: a1Fg8000000AbE1EAK
                    label: Pilot build 1
                    resolutionTimePoint: '2026-04-06T14:31:02.000Z'
                    takenAt: '2026-04-06T14:31:02.000Z'
                    memberCount: 4
        '400':
          description: |
            `INVALID_REQUEST` — unreadable body, or a `label` longer than 80 characters.
            `ASOF_IN_FUTURE` — `cutDateTime` is later than now; a baseline of content that does not
            exist yet is not a baseline. `ASOF_INVALID` — it could not be parsed.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                future:
                  value:
                    code: ASOF_IN_FUTURE
                    message: 'A timepoint cannot be in the future. Use now or an earlier moment. (2027-01-01T00:00:00Z)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFoundPath' }
        '422':
          description: '`EBOM_CYCLE` — the source eBOM contains a cycle, so nothing can be resolved to cut.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '500': { $ref: '#/components/responses/InternalError' }

  /mbom/baselines/{baselineId}/tree:
    get:
      tags: [Baselines]
      operationId: getBaselineTree
      summary: Read the rows a baseline captured
      description: |
        Re-resolves the branch at the baseline's stored `Resolution_TimePoint__c` and returns the
        rows.

        ## `asOf` is refused, not ignored

        Every other tree read on this API takes one, and a client that has been sending it to
        `/branches/{id}/tree` will send it here. A baseline **pins its own timepoint**, so a second
        one has nothing to answer — accepting it would give the same rows to every value and let a
        caller believe they had asked a temporal question and got a temporal answer. Sending `asOf`
        is a `400 INVALID_REQUEST`.

        `flat` **is** honoured, because that is a shape and not a clock.

        ## Differences from the branch read

        - **Excluded rows are filtered out.** A tree read renders a tombstone so the editor can see
          what was removed; a baseline is the record of what the branch *contained*.
        - **`edgeId`, `edgeKey` and the per-row governing clock are suppressed** — a baseline row
          does not pretend to answer questions it cannot. `isReplaced` **is** populated: leaving it
          null would render a substituted part identically to an ordinary one.
        - **`warnings` carries drift only.** A `mixed-revision` warning is advice about a branch
          someone is still editing; a baseline is a record of a decision already taken, so it is
          deliberately not forwarded.

        ## The drift canaries

        | warningCode | What moved |
        |---|---|
        | `BASELINE_COUNT_DRIFT` | the baseline re-derived a **different number of rows** than its cut recorded — a corrected release date, an edit to an already-released BOM |
        | `BASELINE_PROVENANCE_DRIFT` | the same rows came back with a **different number of them claimed** — an override severed or added since the cut. The row count cannot see this, because severing one moves no row |

        Both **warn rather than throw**: the rows returned are still the honest answer for the
        stored timepoint. Each comparison is skipped when the cut never recorded its counter, so a
        baseline taken before a counter existed does not warn on every read.

        Neither is complete, and the response does not pretend otherwise. A same-second revision of
        a claimed row's quantity moves no count on either axis, and an edit to an inherited line's
        own fields moves none either. Both gaps have one cause — a whole-second `Datetime` used as
        the fold's boundary — and closing it needs a monotonic log position rather than a third
        counter.
      parameters:
        - $ref: '#/components/parameters/BaselineId'
        - $ref: '#/components/parameters/Flat'
      responses:
        '200':
          description: The re-resolved rows. Empty `rows` for a baseline that resolved to nothing is a `200`, not an error.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BaselineTree' }
              examples:
                clean:
                  summary: No drift
                  $ref: '#/components/examples/BaselineTreeClean'
                drifted:
                  summary: Count drift after a direct edit to an already-released eBOM
                  $ref: '#/components/examples/BaselineTreeDrift'
        '400':
          description: '`INVALID_REQUEST` — `asOf` was supplied. A baseline is already resolved.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                asOfRefused:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (a baseline is already resolved, so asOf has nothing to answer; read the branch if you want a timepoint)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFoundPath' }
        '500': { $ref: '#/components/responses/InternalError' }

  # ==================================================================== pull

  /mbom/sources/{sourceId}/pull-preview:
    get:
      tags: [Pull]
      operationId: previewMbomPull
      summary: Preview the effect of accepting a newer eBOM revision
      description: |
        Diffs the eBOM resolved at the source's current T_R against the eBOM resolved at the
        candidate root revision's release date, and returns every difference as an individually
        addressable entry.

        **This call is pure.** It creates and modifies nothing — no `Branch_Member__c`, no
        `CI_Rev__c`, no source revision. Calling it twice with the same inputs against an unchanged
        org returns identical output, including identical `entryId` values.

        **Not every entry is a decision.** Most are informational: the caller is being told what will
        happen, not asked. Only `override-orphaned` blocks a commit.

        **Already current is not an error.** If the branch already resolves against the newest
        released root revision, this returns `200` with `hasChanges: false` and an empty `entries`
        array — correct for a read, where a write would throw.

        ## The correlation key — why a pull doesn't orphan every claim on every release

        The two traversals are correlated on **`(Parent_Item__c, Item__c, Secondary_Unique_Key__c)`**
        — the legacy `Assembly__c.Tree_Unique__c` with the parent *revision* swapped for the parent
        *item*. **Not** on `Assembly_CI__c.Edge_Key__c`.

        This is the load-bearing decision of the whole feature. When a parent item revs, Propel
        clones its entire BOM into new `Assembly__c` rows with new ids — so a branch that claimed a
        line under rev-1 holds a line id that does not exist under rev-2. A join on `Edge_Key__c`
        would orphan every claimed edge and re-add every upstream line **on every single pull**. The
        parent item does not move when its revision does, which is exactly what lets this key survive
        the clone. `Edge_Key__c` still identifies the edge *for display*; it just is not the join key.

        `Secondary_Unique_Key__c` rather than `Find_Number__c` because find-number uniqueness is org
        configuration, and a key built on it would give two orgs with identical data different diffs.

        ## The two sides are deliberately asymmetric

        - **current** — the branch resolved at the accepted T_R, *with* its own deltas overlaid.
        - **candidate** — the **raw** upstream walk at the candidate T_R, with **no overlay at all**.

        Overlaying the branch onto the candidate side would match nothing (the deltas are keyed on
        line ids that do not exist under the new root) and would append every delta as an unmatched
        local edge — which **resurrects the orphans**, correlating a claim on a removed line with the
        branch's own edge so `override-orphaned` is never raised. That entry is the only required
        decision in the entire feature; silently losing it would make a commit that should block
        succeed.

        ## Entry kinds

        | kind | decision | availableActions | Meaning |
        |---|---|---|---|
        | `source-version-advance` | informational | — | The root item revision advances; nothing structural beneath it changed. |
        | `member-version-advance` | informational | — | An edge's child resolves to a newer released revision because T_R moved. |
        | `member-added` | informational | — | The candidate adds an edge the branch has no native edge on. Enters automatically. |
        | `member-removed` | informational | — | The candidate removes an edge the branch never claimed or replaced. Applies automatically. |
        | `override-retained` | informational | — | The branch's override still differs from upstream; the branch wins and upstream is suppressed. `retargeted` distinguishes a suppressed **field value** from a suppressed **re-target**. |
        | `override-superseded` | informational | — | The branch's override now *matches* what the candidate would natively produce. |
        | `override-orphaned` | **required** | `convert-to-native`, `remove` | The candidate removes an edge the branch **claimed or replaced**. **Blocks the commit.** |
        | `member-add-collision` | informational | — | The candidate adds an edge whose correlation key coincides with a branch-**native** edge, and the two **differ**. The branch's content wins automatically and is left untouched. |
        | `member-add-converged` | informational | — | The same collision, except the candidate's quantity, find number **and** resolved child revision are all identical to the branch's own. The commit retires the branch's now-redundant local edge (`action: "retired"`); the position becomes ordinary inherited content. |

        `member-add-collision` and `member-add-converged` are the same coincidence resolving two
        ways. Converged requires all three of quantity, find number and resolved child revision to
        match; any difference makes it a plain collision. Neither is ever a decision: on a
        collision nothing is written and the branch's row stands, and on a converged entry the
        commit retires the branch's local edge. An `override-superseded` override is never retired.

        ### Why a revision advance is never a decision

        A revision is not an independently assertable value. Once you accept that the *line* exists,
        which revision it resolves to is a deterministic output of the as-of traversal against T_R —
        there is no alternative to offer. The only way to freeze a revision is `claim`, a separate
        and heavier operation that takes ownership of the whole edge.

        ### Why upstream re-pointing an edge at a *different item* is two entries

        The correlation key includes the child item, so it never correlates as one matched pair. It
        decomposes into an independent `member-removed` (or `override-orphaned`) and an independent
        `member-added`. `retargeted: true` therefore means a different child **revision**, not a
        different child item.

        ## Truncation

        `entriesLimit` caps how many **informational** entries come back. **`required` entries are
        never truncated** — they block the commit, so hiding one would silently conceal a blocker.
        `summary` always reports the *untruncated* totals, and `diffFingerprint` always covers the
        complete diff, so a truncated response is still safe to commit from.

        This call is pure and deterministic, so retrieving more needs no cursor: re-issue with a
        larger `entriesLimit` and get back a longer, stable prefix of the same diff.

        ## Access

        **Gated on write, not read.** A read-only user is rejected before a pull is even previewed —
        the same `Branch_Source__c` update grant `pull` itself requires.
      parameters:
        - $ref: '#/components/parameters/SourceId'
        - name: candidateRootRevisionId
          in: query
          required: false
          description: |
            The released `Item_Revision__c` of the source's root item to preview accepting. When
            omitted, defaults to that item's **latest released revision**.

            Must belong to the same root item the source already references, must be Released (and
            not Cancelled or Expired — the same test that applies when an MBOM is first created), and
            must not resolve to a timestamp at or earlier than the source's current T_R.
          schema:
            $ref: '#/components/schemas/SalesforceId'
        - $ref: '#/components/parameters/EntriesLimit'
      responses:
        '200':
          description: |
            The computed diff, returned bare. Also returned, with `hasChanges: false` and no entries,
            when the branch is already current.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PullPreview' }
              examples:
                changesPending:
                  summary: Assy-100 rev-1 → rev-2, one blocking orphan
                  $ref: '#/components/examples/PullPreviewWithChanges'
                alreadyCurrent:
                  summary: Branch already on the newest released revision
                  $ref: '#/components/examples/PullPreviewNoChanges'
                truncated:
                  summary: Informational entries capped; the required one survives
                  $ref: '#/components/examples/PullPreviewTruncated'
        '400':
          description: |
            `INVALID_REQUEST` — `entriesLimit` was not a positive integer. Present-but-unusable is a
            400 rather than a silent fallback to the default.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                badLimit:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (entriesLimit 0)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: '`NOT_FOUND` — the source, or `candidateRootRevisionId`, does not exist.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                notASource:
                  value:
                    code: NOT_FOUND
                    message: 'That record does not exist, or you do not have access to it. (a1Bg8000000AbCdEAK is not a Branch_Source__c)'
        '409': { $ref: '#/components/responses/PullConflictPreview' }
        '422': { $ref: '#/components/responses/PullUnprocessable' }
        '500': { $ref: '#/components/responses/InternalError' }

  /mbom/sources/{sourceId}/pull:
    post:
      tags: [Pull]
      operationId: commitMbomPull
      summary: Commit a previewed pull, advancing T_R
      description: |
        Accepts a newer eBOM root revision into the branch: appends a new `Branch_Source_Rev__c`
        advancing T_R, advances the source's root pointer, applies the caller's `override-orphaned`
        dispositions, and retires any `member-add-converged` local edge — **all under one savepoint**.

        Returns **`201 Created`**, not 200: a new acceptance record genuinely exists.

        ## The diff is never trusted from the client

        This endpoint **re-resolves the same diff server-side**, through the same code path the
        preview used, and answers `STALE_REVISION` the moment the re-resolved `diffFingerprint`
        disagrees with what you echoed back, or an `entryId` in `decisions[]` no longer names an
        entry in it. Re-preview and resubmit.

        A useful side effect: every preview-time failure mode (`NOT_RELEASED`,
        `CANDIDATE_ROOT_MISMATCH`, `CANDIDATE_NOT_NEWER`, `CYCLE_DETECTED`, `NOT_FOUND`) applies here
        for free — nothing in the commit path re-implements them.

        ## Decisions

        | Situation | Result |
        |---|---|
        | An `override-orphaned` entry with **no** decision | `409 DISPOSITION_REQUIRED` — the whole commit is rejected. There is no default-accept tier. |
        | A decision naming any **other** kind | `400 INVALID_REQUEST` — no other kind is ever actionable. |
        | An unrecognised `action` | `400 INVALID_REQUEST`. |

        | action | Effect |
        |---|---|
        | `convert-to-native` | Severs the provenance link, keeping the branch-owned content, which then resolves on the **branch** clock. Runs through the ordinary `convert-to-native` delta operation — every invariant that batch enforces applies unchanged. |
        | `remove` | Appends an **exclude** delta (a deletion marker). **Nothing is hard-deleted.** |

        ## Override reconciliation: reported, never persisted

        For every existing claimed/replaced field, the commit compares the override's current value
        against what the candidate traversal would natively produce:

        - **Differs** → reported as `override-retained` / **reaffirmed**. It stays active and
          continues to govern future pulls. **Nothing new is persisted** — the comparison is
          reconstructed from the accepted source version on every pull, so a "reaffirmation" record
          would change no rendered value.
        - **Matches** → reported as `override-superseded`. Also **never deleted and never written**.
          Retiring it would mean a branch that once happened to agree with upstream would silently
          start accepting upstream changes from then on.

        `Edge_Key__c` and `Alias__c` are likewise **never re-stamped**. They keep meaning "the
        upstream line this edge was originally minted against", independent of the correlation key a
        pull actually joins on.

        ## Retiring a converged local add

        A `member-add-converged` entry is retired without a decision. "Retired" means **one appended
        `Branch_Member__c` row of `Delta_Type__c = 'Revert'`**, with `Overrides_Source__c` null —
        nothing is deleted, and a read dated before this commit still renders the local add. The
        position becomes ordinary inherited content pinned at the new T_R, so it follows the source
        from then on.

        Reported in `applied[]` as `{"kind": "member-add-converged", "action": "retired"}`, inside
        the same savepoint as the rest of the commit.

        ## Atomicity

        One outer savepoint around both the disposition batch and the acceptance insert, rolled back
        on any exception from either. A failure part way leaves the prior accepted revision and T_R
        in place, with no orphaned claim, exclude or override-status records.

        ## The already-current commit

        A commit submitted when the candidate equals the currently-accepted root **still succeeds**:
        it appends a new acceptance (advancing `Accepted_At__c` with the same `Source_TimePoint__c`)
        and returns `applied: []`. Same reading the preview takes of the read side.

        ## What a pull leaves alone

        Branch-**native** content is unaffected, with one exception: the `member-add-converged`
        retirement above. A local add that still differs from the source (`member-add-collision`) is
        left untouched, as is native content the source never collided with.

        ## Access

        Requires **create** on `Branch_Source_Rev__c`, `Branch_Member__c`, `CI__c`, `CI_Rev__c`,
        `Assembly_CI__c` and `Assembly_CI_Rev__c`, **and edit** on `Branch_Source__c`,
        `Assembly_CI__c` and `CI_Rev__c`. Both gates run **before the body is read**.
      parameters:
        - $ref: '#/components/parameters/SourceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PullCommitRequest'
            examples:
              withDisposition:
                summary: Keep an orphaned claim as branch-owned content
                value:
                  candidateRootRevisionId: a0Ag8000000Ab02EAC
                  diffFingerprint: 'sha256:9f2c41ab7e08d3b6'
                  decisions:
                    - entryId: f4880c6b3e1a9d75
                      action: convert-to-native
              removeOrphan:
                summary: Let the orphaned claim go
                value:
                  candidateRootRevisionId: a0Ag8000000Ab02EAC
                  diffFingerprint: 'sha256:9f2c41ab7e08d3b6'
                  decisions:
                    - entryId: f4880c6b3e1a9d75
                      action: remove
              noDecisions:
                summary: A diff with no required entries
                value:
                  candidateRootRevisionId: a0Ag8000000Ab02EAC
                  diffFingerprint: 'sha256:1b7e3390cc02af41'
                  decisions: []
      responses:
        '201':
          description: The pull was committed. A new `Branch_Source_Rev__c` now exists.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PullCommitResult' }
              examples:
                committed:
                  $ref: '#/components/examples/PullCommitCommitted'
                alreadyCurrent:
                  summary: Committing the revision already accepted
                  $ref: '#/components/examples/PullCommitAlreadyCurrent'
        '400':
          description: |
            `INVALID_REQUEST` — the body could not be read; a decision names an entry that is not
            `override-orphaned`; or `action` is not one of `convert-to-native` / `remove`.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                notActionable:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (entryId b7204de9a1c6f358 is member-added, which is never actionable)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: '`NOT_FOUND` — the source, or the candidate revision, does not exist.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '409': { $ref: '#/components/responses/PullConflictCommit' }
        '422': { $ref: '#/components/responses/PullUnprocessable' }
        '500': { $ref: '#/components/responses/InternalError' }

  # ==================================================================== comparison

  /mbom/branches/{branchId}/comparison:
    get:
      tags: [Comparison]
      operationId: compareBranch
      summary: Compare a branch against another configuration
      description: |
        The structural difference between two configurations: which members each holds, and where
        the two disagree on quantity, find number or the child revision they resolve to.

        The **left** side is the branch the path names. The **right** side is named by
        `rightType` + `rightId`, and may be another branch, a baseline, a source, or a released
        eBOM revision — so one endpoint answers eBOM-against-MBOM, MBOM-against-MBOM and
        MBOM-against-its-own-baseline rather than three.

        **This call is pure.** No DML anywhere behind it. Two calls with the same inputs against an
        unchanged org return identical output, `entryId` values included.

        ## The right-hand side

        | `rightType` | `rightId` names | Resolves to | Its clock |
        |---|---|---|---|
        | `branch` | `Branch__c` | the branch's own effective structure, deltas overlaid | `rightAsOf`, defaulting to now |
        | `baseline` | `Baseline__c` | the branch re-resolved at the boundary the cut recorded | the baseline's frozen `Resolution_TimePoint__c`; `rightAsOf` is **refused** |
        | `source` | `Branch_Source__c` | what that source *supplies*, with no branch deltas folded in | the acceptance in effect at `rightAsOf` |
        | `ebom` | `Item_Revision__c` | a raw eBOM walk beneath that revision, no branch in the picture | `rightAsOf`, defaulting to **that revision's release date** |

        `rightType` is matched **case-insensitively** — `rightType=Branch` is a 200. The values
        are published lower-case and that is the spelling to send; normalising a published wire
        value is a courtesy the HTTP surface affords, and the service behind it stays
        case-sensitive.

        A `source` with nothing accepted yet resolves to no root and no members. That is a `200`,
        not an error: every member of the other side lands as added or removed.

        ## Entry kinds

        | kind | Meaning |
        |---|---|
        | `root-version-differs` | The two sides resolve **different root revisions**. One entry per comparison, never one per edge. |
        | `member-added` | The correlation key is present on the **right** and absent on the left. |
        | `member-removed` | The correlation key is present on the **left** and absent on the right. |
        | `member-differs` | Present on both, content disagrees. `differingFields[]` names which of `quantity` / `findNumber` / `childRevision`. |
        | `member-unchanged` | Present on both and identical. **Emitted only when `includeUnchanged=true`.** |

        One entry per correlation key, always — which is why `member-differs` carries a list rather
        than splitting into three kinds. `entryId` hashes `(kind, parent item, child item,
        secondary unique key)`, so two entries on one key would collide.

        `root-version-differs` fires whenever the two **resolved root revisions** differ, and that
        includes the case where the two roots are different *items* — a different item's revision
        is trivially a different revision. That is what stops `identical` reporting two unrelated
        assemblies as the same configuration. The abnormal shape is named separately, by the
        `COMPARE_ROOTS_DIFFER` warning.

        ### The correlation key is the pull's key

        `(Parent_Item__c, Item__c, Secondary_Unique_Key__c)` — the same join
        [pull-preview](#operation/previewMbomPull) documents in full, for the same reason: the
        parent *item* does not move when its revision does, so the key survives the BOM clone a
        revision advance performs. Two edges on **one side** sharing a key is
        `AMBIGUOUS_CORRELATION` (422) rather than a guess.

        ### Exclusion tombstones are filtered out before the join

        Both sides are reduced to what the configuration *contains* — the same
        `isContainedMember` predicate a baseline read applies — so a tombstone never becomes a
        member. A member excluded on the left therefore surfaces as **`member-added`** against a
        right that still supplies it: the left does not contain it and the right does, which is the
        honest answer for a symmetric compare and what keeps branch-against-baseline
        self-consistent, since a baseline's rows were filtered by that same predicate when it was
        cut.

        ### Provenance is reported, never classified

        Each entry's `left` / `right` state carries `provenance` and `governingClock`, so a
        claimed-versus-inherited difference is visible without a kind of its own. A provenance-only
        difference is **not** a content difference and produces no entry: the same part at the same
        quantity in the same position is the same member however each side came to hold it. A
        branch that claimed a member without changing it therefore reads as unchanged against its
        own source.

        ## What a comparison deliberately does not have

        - **No decision tier and no actions.** A comparison initiates nothing, so `decision`,
          `availableActions`, `retargeted` and `overrideKind` are **absent** from an entry rather
          than defaulted — a consumer cannot read a default as an answer. That is also why
          `CompareEntry` is not the pull's `DiffEntry`, whose `current` / `candidate` naming would
          be a lie on a symmetric pair.
        - **No `diffFingerprint`.** The pull carries one so a commit can fail fast on a stale
          preview. There is no commit here, so a digest would be a field nobody could act on.

        ## `summary`, and the one thing that is easy to get wrong

        `total` and `byKind` count the entries **emitted**, so both move with `includeUnchanged`.
        `added`, `removed`, `differing` and **`unchanged` do not** — `unchanged` is derived from the
        matched correlation keys and is therefore **populated whether or not the unchanged rows were
        asked for**. That is the point of it: the default response emits no row for an identical
        member, and a caller still needs to know how much of the configuration matched without
        paying for the rows. So `summary.unchanged` does **not** always equal
        `byKind['member-unchanged']`; with `includeUnchanged=false` the latter is `0`.

        `byKind` is **pre-seeded with all five kinds at zero**, unlike the pull's summary, which
        carries only the kinds it observed. A comparison is read by a client rendering a fixed set
        of counters, and an absent key would make every one of them a null check.

        Counts are always over the **untruncated** comparison.

        ## Ordering and truncation

        Entries are ordered by **breadcrumb path, then find number, then child item id, then kind**,
        with `entryId` last purely as a total-order guarantee. A deliberate divergence from the
        pull's `entryId`-hash order: a pull preview is read as a flat decision list, while a
        comparison is read against a tree, and hash order scatters siblings.

        `entriesLimit` caps `entries` as a **plain prefix** — unlike the pull, a comparison has no
        `required` tier that trimming could silently hide. `summary` is left alone, which is what
        makes `truncated: true` actionable rather than merely true; re-issue with a larger limit to
        get a longer, stable prefix of the same comparison.

        The default cap is **not** raised by `includeUnchanged=true`, deliberately: a 500-edge BOM
        truncates at 200 and says so, rather than silently returning a response an order of
        magnitude larger than the caller expected.

        > Truncation is applied by the REST layer. The service beneath it always returns a complete
        > comparison, so a Flow or an LWC controller calling it directly sees every entry with
        > `truncated: false` and `entriesLimit: null`. Over HTTP both fields are always filled in.

        ## Access

        **Gated on read, not write** — deliberately unlike
        [pull-preview](#operation/previewMbomPull), which requires the same write grant the commit
        does because previewing a pull is initiating one. A comparison initiates nothing, and
        refusing a read-only user the ability to *look* at a difference has no justification.
        Confirmed with the PM: read-only users may compare. This is not a copied-and-forgotten gate.

        Each side is gated on the object it is addressed by: `Branch__c` for a branch,
        `Baseline__c` **and** `Branch__c` for a baseline, `Branch_Source__c` for a source,
        `Item_Revision__c` for an eBOM revision.
      parameters:
        - $ref: '#/components/parameters/BranchId'
        - $ref: '#/components/parameters/RightType'
        - $ref: '#/components/parameters/RightId'
        - $ref: '#/components/parameters/AsOf'
        - $ref: '#/components/parameters/RightAsOf'
        - $ref: '#/components/parameters/IncludeUnchanged'
        - $ref: '#/components/parameters/EntriesLimit'
      responses:
        '200':
          description: 'The comparison. An identical pair is a `200` with `identical: true`, not an empty response and not an error.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Comparison' }
              examples:
                identical:
                  summary: A branch against its own source — no differences
                  $ref: '#/components/examples/ComparisonIdentical'
                differences:
                  summary: A branch against a newer released eBOM revision
                  $ref: '#/components/examples/ComparisonWithDifferences'
                includingUnchanged:
                  summary: The same comparison with `includeUnchanged=true`
                  $ref: '#/components/examples/ComparisonIncludingUnchanged'
                truncated:
                  summary: The same comparison at `entriesLimit=2`
                  $ref: '#/components/examples/ComparisonTruncated'
        '400':
          description: |
            `INVALID_REQUEST` — `rightType` is missing or is not one of the four values;
            `rightId` was not supplied; `entriesLimit` is not a positive integer (`entriesLimit=0`
            is refused, **not** silently defaulted); or `rightAsOf` was supplied for a baseline
            right side, which resolves at its own recorded timepoint and could not honour a
            different one.

            `ASOF_INVALID` / `ASOF_IN_FUTURE` — either clock could not be parsed, or names a moment
            that has not happened.

            An unrecognised `includeUnchanged` is **not** an error — see that parameter.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                unknownRightType:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (rightType must be one of {baseline, branch, ebom, source}, not revision)'
                zeroLimit:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (entriesLimit 0)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: |
            `NOT_FOUND` — the path id names no branch or names another object; `rightId` names
            nothing, or names an object other than the one `rightType` implies; or the path
            carries an unrecognised trailing segment (`.../comparison/junk`).

            A **missing** `rightId` is a 400 and a **present but unusable** one is a 404: the
            first is half a request, the second names a resource that is not there.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                rightIdWrongObject:
                  value:
                    code: NOT_FOUND
                    message: 'That record does not exist, or you do not have access to it. (a1Cg8000000AbCeEAK is not a Branch__c)'
        '409': { $ref: '#/components/responses/ComparisonConflict' }
        '422': { $ref: '#/components/responses/ComparisonUnprocessable' }
        '500': { $ref: '#/components/responses/InternalError' }

  /mbom/baselines/{baselineId}/comparison:
    get:
      tags: [Comparison]
      operationId: compareBaseline
      summary: Compare a baseline against another configuration
      description: |
        The same comparison as [`compareBranch`](#operation/compareBranch) — same right-hand types,
        same kinds, same summary, same truncation — with a **baseline** on the left instead of a
        branch.

        It exists because a baseline has to be able to sit on the left as well as the right: "what
        has this branch done since we froze it?" and "what changed between these two builds?" are
        both baseline-rooted questions, and neither can be asked by naming the baseline on the right
        alone.

        ## `asOf` is refused here, not ignored

        The left side is a baseline, so it resolves at the boundary its cut recorded. A second
        clock has nothing to answer, and accepting one would let a caller believe they had asked a
        temporal question and been given a temporal answer. Sending `asOf` is a
        `400 INVALID_REQUEST` — the same reading
        [`getBaselineTree`](#operation/getBaselineTree) takes. `rightAsOf` is still accepted,
        unless the *right* side is a baseline too.

        There is deliberately **no** `sources/{id}/comparison` and no eBOM-rooted left side. Both
        can still be compared — as the right-hand side of a branch or baseline — and eBOM against
        eBOM is already served by Propel's existing BOM Compare, which a third surface would
        duplicate.

        ## Access

        Read, not write, exactly as the branch-rooted endpoint. A baseline side is gated on
        `Baseline__c` **and** `Branch__c`, because reading one resolves the branch behind it.
      parameters:
        - $ref: '#/components/parameters/BaselineId'
        - $ref: '#/components/parameters/RightType'
        - $ref: '#/components/parameters/RightId'
        - $ref: '#/components/parameters/RightAsOf'
        - $ref: '#/components/parameters/IncludeUnchanged'
        - $ref: '#/components/parameters/EntriesLimit'
      responses:
        '200':
          description: The comparison, in the shape `compareBranch` returns — `left.type` is `baseline`.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Comparison' }
        '400':
          description: |
            `INVALID_REQUEST` — everything `compareBranch` refuses, **plus `asOf`**, which a
            baseline left side cannot honour.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
              examples:
                asOfRefused:
                  value:
                    code: INVALID_REQUEST
                    message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (asOf cannot be supplied for a baseline: a baseline resolves at its own recorded timepoint, and honouring a different one would not be that baseline)'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: '`NOT_FOUND` — the path id names no baseline, `rightId` names nothing or the wrong object, or the path carries a trailing segment.'
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestError' }
        '409': { $ref: '#/components/responses/ComparisonConflict' }
        '422': { $ref: '#/components/responses/ComparisonUnprocessable' }
        '500': { $ref: '#/components/responses/InternalError' }

# ======================================================================== components

components:

  securitySchemes:
    sessionToken:
      type: http
      scheme: bearer
      description: |
        Standard Salesforce OAuth 2.0 access token or session id, sent as
        `Authorization: Bearer <token>`.

  parameters:

    BranchId:
      name: branchId
      in: path
      required: true
      description: |
        Id of the `Branch__c`. Returned as `branchId` from `POST /mbom/mboms`.

        A malformed id, or a well-formed id of the wrong object, is a **404** — not a 500 and not a
        403. The URI is what names the resource, so a path naming nothing is a path to nothing, and
        the two tree endpoints therefore cannot be confused for each other by a caller that swaps a
        branch id for a source id.
      schema: { $ref: '#/components/schemas/SalesforceId' }

    SourceId:
      name: sourceId
      in: path
      required: true
      description: |
        Id of the `Branch_Source__c`. Returned as **`sourceId`** from `POST /mbom/mboms`.

        In Phase 1 a branch has exactly one source, of `Source_Type__c = 'Ebom'`. The owning branch is
        reached through `Branch_Source__c.Downstream_Branch__c`, which is why there is no separate
        `branchId` segment on these paths.
      schema: { $ref: '#/components/schemas/SalesforceId' }

    BaselineId:
      name: baselineId
      in: path
      required: true
      description: Id of the `Baseline__c`, returned from the cut.
      schema: { $ref: '#/components/schemas/SalesforceId' }

    AsOf:
      name: asOf
      in: query
      required: false
      description: |
        The **branch** clock for this read. Defaults to the server clock when omitted; a future value
        is `ASOF_IN_FUTURE` (400).

        Governs the branch's own deltas, local content and substitutes. It does **not** govern
        inherited content, which always resolves at T_R — that is the two-clock model, and passing
        `asOf` to the inherited traversal is the single change that would quietly make a pinned MBOM
        float.

        Accepts ISO-8601 (`2026-02-01T00:00:00Z`, `2026-02-01T00:00:00+05:30`) and the
        space-separated Apex form (`2026-02-01 03:04:05`, interpreted as GMT). A `+` offset is
        repaired server-side after query-string decoding turns it into a space, so a legal ISO offset
        does not fail in transport.
      schema:
        type: string
        format: date-time
      examples:
        iso: { value: '2026-02-01T00:00:00Z' }

    Flat:
      name: flat
      in: query
      required: false
      description: |
        Response shape. **Flat is the default, and only an explicit `flat=false` nests.**

        Anything unrecognised (`flat=0`, `flat=no`) falls back to flat rather than raising — partly
        because `Boolean.valueOf('0')` is `false` and the obvious implementation would *nest* for
        `flat=0`, the opposite of what that caller asked for, and partly because a flat response
        carries `parentItemId` on every row and is therefore losslessly re-nestable by the client.

        Not accepted by `GET /mbom/sources/{sourceId}/tree` — silently ignored there, so one
        parameter set can be sent to both tree reads.
      schema:
        type: boolean
        default: true

    EntriesLimit:
      name: entriesLimit
      in: query
      required: false
      description: |
        Caps how many **`informational`** entries the preview returns. `required` entries are always
        returned in full regardless of this cap.

        Must be a positive integer; anything else is `400 INVALID_REQUEST` rather than a silent
        fallback to the default.
      schema:
        type: integer
        minimum: 1
        default: 200

    RightType:
      name: rightType
      in: query
      required: true
      description: |
        What the **right-hand** side of the comparison is. Required — a comparison against nothing
        in particular is half a request, so a missing or unrecognised value is
        `400 INVALID_REQUEST`.

        - `branch` — another `Branch__c`, resolved with its own deltas overlaid.
        - `baseline` — a `Baseline__c`, replayed at the boundary its cut recorded.
        - `source` — a `Branch_Source__c`: what that source *supplies*, with no branch deltas
          folded in. This is the "what have I changed relative to upstream?" comparison.
        - `ebom` — a released `Item_Revision__c`, walked raw, with no branch in the picture.

        **Matched case-insensitively**, so `Branch` and `BRANCH` are both accepted. Send the
        lower-case spelling: that is the published value, and it is what comes back in
        `right.type`.
      schema:
        $ref: '#/components/schemas/CompareSideType'

    RightId:
      name: rightId
      in: query
      required: true
      description: |
        The record the right-hand side is. Its object must match `rightType` —
        `Branch__c`, `Baseline__c`, `Branch_Source__c` or `Item_Revision__c` respectively.

        **Absent is a 400** (the caller described half a comparison); **present but naming nothing,
        or naming another object, is a 404** (the caller named a resource that is not there). The
        same split the path ids make.
      schema: { $ref: '#/components/schemas/SalesforceId' }

    RightAsOf:
      name: rightAsOf
      in: query
      required: false
      description: |
        The **right** side's read clock. `asOf` governs the left side; each side keeps its own pair
        of clocks and never borrows the other's, which is why there are two parameters rather than
        one. A branch read at now against a baseline frozen in March is two configurations at two
        instants, and both are published on the response.

        Defaults differ by side type — now for `branch` and `source`, and for `ebom` **that
        revision's own release date**, since a raw eBOM walk has no acceptance to pin it and "the
        eBOM of revision X" honestly means the eBOM as it stood when X was released.

        **Refused for a `baseline` right side** (`400 INVALID_REQUEST`), which resolves at the
        timepoint its cut recorded. Parsing and the future-value rule are `asOf`'s, unchanged.
      schema:
        type: string
        format: date-time
      examples:
        iso: { value: '2026-04-02T00:00:00Z' }

    IncludeUnchanged:
      name: includeUnchanged
      in: query
      required: false
      description: |
        Emit a `member-unchanged` entry for every correlation key present and identical on both
        sides, turning the response into the **complete merged BOM** — one entry per key across
        both sides, which is what a side-by-side tree renders from a single call.

        Defaults to `false`. **Only an explicit `true` (any case) turns it on**; anything else —
        `includeUnchanged=1`, `=yes` — falls back to the default rather than raising, because the
        fallback is the smaller response and loses no information: `summary.unchanged` carries the
        count either way.

        It costs no extra query. It does **not** raise `entriesLimit`, and it moves `summary.total`
        and `summary.byKind`, which count entries emitted — see the endpoint description.
      schema:
        type: boolean
        default: false

  # ---------------------------------------------------------------- schemas

  schemas:

    SalesforceId:
      type: string
      pattern: '^[a-zA-Z0-9]{15}([a-zA-Z0-9]{3})?$'
      description: A 15- or 18-character Salesforce record Id.
      examples: ['a1Bg8000000AbCdEAK']

    # -------------------------------------------------- shared vocabulary

    Provenance:
      type: string
      description: |
        Where a rendered row comes from. **Derived at read time, never stored** — `claimed` and
        `local` differ by exactly one field on the winning delta row.

        - `inherited` — supplied by the source; no branch-owned record exists.
        - `claimed` — the branch took copy-on-write ownership of a source-supplied member.
        - `local` — branch-owned with no upstream provenance ("native").
        - `nested` — a descendant of a locally added or substituted assembly, rendered from that
          item's own eBOM. Read-only: every row in such a subtree carries `isEditable: false`, and
          the pull diff excludes them entirely. Appears on tree rows only, never in a diff entry's
          `MemberState`.

        There is no `excluded` value: exclusion is tracked as a separate boolean alongside
        provenance, never as a value of it.
      enum: [inherited, claimed, local, nested]

    GoverningClock:
      type: string
      description: |
        Which clock resolves this member. `source` is the pinned T_R (it only advances when a pull is
        committed); `branch` is the caller's `asOf`. Both are legitimately in play at once, which is
        what makes mixed revisions of one item possible.
      enum: [source, branch]

    CiKind:
      type: string
      description: |
        Discriminator for the configuration-item type, and therefore for the shape of
        `MemberState.detail`. `assembly-edge` is the only member type implemented; this enum is the
        extension point where a future CI type is added.

        `source` appears only on the root-level `source-version-advance` entry's `target` (and on the
        root segment of every `path[]`), because that entry is about the source's root **item**
        rather than an edge beneath it: it has no parent, no quantity and no find number.
      enum: [assembly-edge, source]

    # -------------------------------------------------- create

    MbomCreateRequest:
      type: object
      required: [rootItemRevisionId, mbomName, branchLabel]
      properties:
        rootItemRevisionId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: |
            The **released** `Item_Revision__c` the MBOM is built over. Its `Released_Date_Time__c`
            becomes T_R.
        mbomName:
          type: string
          maxLength: 80
          description: |
            `MBOM__c.Name`. Required, and a blank value is refused with `INVALID_REQUEST` rather than
            being left to DML — a custom object's Text `Name` is unenforced by the API, so a blank
            one would silently produce an MBOM named after its own record id that nobody can search
            for.
          examples: ['Assy-100 Manufacturing BOM']
        branchLabel:
          type: string
          maxLength: 80
          description: |
            Lands on **both** `Branch__c.Name` (what every Salesforce list view and lookup shows) and
            `Branch__c.Label__c`. Bounded by the shorter of the two fields.
          examples: ['MBOM-GDL']

    MbomCreateResult:
      type: object
      required: [mbomId, branchId, sourceId, sourceRevisionId]
      properties:
        mbomId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The `MBOM__c` header.
        branchId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The initial `Branch__c`. Use it on every `/branches/...` endpoint.
        sourceId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: |
            The `Branch_Source__c` linking the branch to its eBOM. **This is the `sourceId`** on
            every `/sources/...` endpoint.
        sourceRevisionId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The first acceptance, carrying T_R in `Source_TimePoint__c`.

    # -------------------------------------------------- trees

    StructureTree:
      type: object
      description: The response body of **both** tree reads — a branch's effective structure, and a source's structure on its own.
      required: [rootRevisionId, rows, warnings]
      properties:
        asOf:
          type: string
          format: date-time
          description: The timepoint this tree was resolved at, echoed so the response is self-describing. Present even when the caller sent none.
        rootRevisionId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The root `Item_Revision__c` the structure hangs from.
        sourceTimePoint:
          type: string
          format: date-time
          description: '**T_R** — the source timepoint inherited rows are pinned at.'
        rows:
          type: array
          description: Always present; empty rather than absent.
          items: { $ref: '#/components/schemas/TreeRow' }
        warnings:
          type: array
          description: Always present; empty rather than absent.
          items: { $ref: '#/components/schemas/TreeWarning' }

    TreeRow:
      type: object
      description: |
        One row of a resolved tree. The same shape serves both response shapes: in the **nested**
        shape `childRows` holds the subtree and `parentItemId` is still populated; in the **flat**
        shape every row is a sibling and `childRows` is empty — so a client can rebuild the tree from
        a flat response without a second request.
      required: [provenance, childRows]
      properties:
        edgeId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `Assembly_CI__c` edge. **Null for a purely inherited row** that no branch has claimed. Suppressed on baseline rows.
        edgeKey:
          oneOf: [{ type: string }, { type: 'null' }]
          description: |
            The legacy `Assembly__c` line id, or a synthetic key for branch-local content:
            `local-{branchId}-{parentItemId}-{childItemId}` for an `add`, or
            `replaces-{branchId}-{supersededEdgeKey}` for a substitute. Suppressed on baseline rows.
          examples: ['a0Bg8000000Ab10EAC']
        detail:
          $ref: '#/components/schemas/AssemblyEdgeDetail'
        provenance: { $ref: '#/components/schemas/Provenance' }
        governingTimestamp:
          oneOf: [{ type: string, format: date-time }, { type: 'null' }]
          description: T_R for an inherited row, `asOf` for a local or substituted one. **Null on baseline rows**, where it is suppressed.
        sourceId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `Branch_Source__c` this row came from. Null for a branch-local row.
        isClaimed: { type: boolean }
        isExcluded:
          type: boolean
          description: |
            True for a tombstoned row. **Rendered by the branch tree read** so the editor can see
            what was removed; **filtered out** by the baseline read.
        isReplaced:
          type: boolean
          description: True when this row stands in for another (`replace`). Populated on baseline rows too — a null would render a substituted part identically to an ordinary one.
        isEditable:
          type: boolean
          description: |
            False on any row inside the subtree of a locally added or substituted assembly. Every
            delta op refuses those rows, so a UI offering an action on one is offering an action the
            API will reject.
        isCollapsedCollision:
          type: boolean
          description: |
            True when one BOM position is asserted by both the branch and its source and the two
            render as a single row, with the branch's content winning. A tree read carries a
            `MEMBER_ADD_COLLISION` warning alongside it.

            Defaults to `false`. Populated on baseline rows too, where — because a baseline
            forwards drift warnings only — it is the sole signal of the collapse.
        childRows:
          type: array
          description: Populated in the nested shape only; **empty, never null**, in the flat shape.
          items: { $ref: '#/components/schemas/TreeRow' }

    TreeWarning:
      type: object
      description: |
        A condition worth telling the user about that does **not** block the read. Shared by the tree
        reads, the baseline read and the pull preview.

        The key is **`warningCode`**, matching the existing mBOM convention — never `code`, which
        this API reserves for the error body.
      required: [warningCode, message]
      properties:
        warningCode:
          type: string
          enum: [MIXED_REVISION, BASELINE_COUNT_DRIFT, BASELINE_PROVENANCE_DRIFT, NESTED_CYCLE_PRUNED, MEMBER_ADD_COLLISION, COMPARE_ROOTS_DIFFER]
          description: |
            The complete vocabulary.

            - `MIXED_REVISION` — one item rendered at more than one revision in the same tree,
              because two clocks are in play. Legal and expected; warned, never blocked.
            - `BASELINE_COUNT_DRIFT` — a baseline re-derived a different **row count** than its cut
              recorded.
            - `BASELINE_PROVENANCE_DRIFT` — a baseline re-derived a different number of **claimed**
              rows than its cut recorded. Separate from the count drift because severing an override
              moves no row: the same rows come back, one of them no longer an override. Narrow —
              only reachable when the severing delta lands in the same whole second as the cut,
              since a saved `Datetime` has second precision.
            - `NESTED_CYCLE_PRUNED` — the subtree of a locally added assembly was dropped because
              walking it reached a cycle. The rest of the tree is still returned.
            - `MEMBER_ADD_COLLISION` — one BOM position was asserted by **both** the branch and its
              source, and the branch's content won. The two rows render as one, carrying
              `isCollapsedCollision: true`. Not raised once a pull has retired a *converged* local
              add, because only the source asserts the position from then on.
            - `COMPARE_ROOTS_DIFFER` — a comparison's two sides are rooted at different **items**,
              so they have no members in common and every entry is an addition or a removal for
              that reason alone. The comparison still answers; refusing would be the wrong call for
              a read. Comparison responses only, where it is reported on **`leftWarnings`** — it is
              about the pair rather than either side, the response has two warning lists and no
              third, and the left is the side the caller addressed by path. It carries no `itemId`
              and no revisions: there is no single item it is about, and `left` / `right` already
              publish both roots.
        message:
          type: string
          description: Human-readable, from a Custom Label. Translated; never parse it.
        itemId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: |
            Present on `MIXED_REVISION` — the item rendered at more than one revision — and on
            `MEMBER_ADD_COLLISION`, naming the child item whose position collapsed.
        itemRevisionIds:
          type: array
          description: Present on `MIXED_REVISION` — the revisions in play.
          items: { $ref: '#/components/schemas/SalesforceId' }
        governingTimestamps:
          type: array
          description: Present on `MIXED_REVISION` — the clock that governed each, positionally aligned with `itemRevisionIds`.
          items: { type: string, format: date-time }

    # -------------------------------------------------- deltas

    DeltaBatchRequest:
      type: object
      required: [operations]
      properties:
        atomic:
          type: boolean
          default: true
          description: |
            All-or-nothing when true. **A null is treated as `true`** — the safer contract wins the
            default, so a caller who did not think about the failure mode gets all-or-nothing rather
            than a half-applied branch.
        asOf:
          type: string
          format: date-time
          description: |
            Resolves the branch **once** for the whole batch; every operation then validates against
            that one snapshot, so a batch cannot see the tree shift underneath it halfway through.

            Defaults to the server clock; never accepted in the future. A **past** value means
            "validate against the configuration as it stood then" — it never dates the write. Several
            operations refuse a past value outright; see the endpoint description.
        operations:
          type: array
          maxItems: 200
          description: At most 200 — beyond that, `BATCH_TOO_LARGE` (413), refused before anything is resolved.
          items: { $ref: '#/components/schemas/DeltaOperation' }

    DeltaOperation:
      type: object
      description: |
        One operation. The shape is a **union across all eight operations** rather than a class per
        operation, because the batch arrives as one JSON array. `op` selects which fields are
        meaningful, and each operation validates its own required fields — an `add` with no
        `parentItemId` is that operation's error to raise, not a deserialisation failure.

        Unknown keys are **silently ignored** (the deserialiser is lenient), so a *mistyped* field
        name reads as a null id. `rootItemRevisionld` with an `l` will answer `NOT_FOUND`, not a
        schema error.
      required: [op]
      properties:
        op:
          type: string
          enum: [claim, update, add, exclude, restore, replace, revert, convert-to-native]
          description: Matched case-insensitively. An unknown value is a **per-operation** validation failure (`INVALID_REQUEST`), not an exception — one mistyped name in a batch of 200 must not read as a server fault.
        clientRef:
          type: string
          description: |
            Opaque caller token, echoed verbatim on this operation's result. Set it to correlate
            results with a UI row; the server never interprets it. Correlating on order alone breaks
            the moment anything is reordered, and on `clientRef` alone breaks when you do not set it
            — so `index` is stated too.
          examples: ['row-motor']
        ebomAssemblyId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: 'The legacy `Assembly__c` line. Used by: `claim`, `exclude`, `update` (the `CLAIM_REQUIRED` path), `revert` (the second-revert path).'
        assemblyCiId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: An existing `Assembly_CI__c` edge to `update`. The ordinary form.
        ciId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: 'A `CI__c` to `exclude`, `restore`, `revert` or `convert-to-native`.'
        parentItemId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: '`add` only — the parent **item** (never a revision).'
        childItemId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: '`add` only — the part being added.'
        replacedEbomAssemblyId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: '`replace` only — the legacy line being superseded. Must be an eBOM-derived row; a branch-local row cannot be replaced.'
        newChildItemId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: '`replace` only — the substitute part. Its revision **floats** at the read clock rather than being pinned.'
        quantity:
          type: number
          description: 'Used by `update`, `add`, `replace`.'
        findNumber:
          type: number
          description: 'Used by `update`, `add`, `replace`.'

    DeltaBatchResponse:
      type: object
      required: [branchId, atomic, asOf, applied, failed, results]
      properties:
        branchId: { $ref: '#/components/schemas/SalesforceId' }
        atomic:
          type: boolean
          description: Echoed, so the response says which contract it was written under.
        asOf:
          type: string
          format: date-time
          description: |
            The timepoint the whole batch resolved against. It matters more here than on a read — the
            caller may have sent none at all, and this is the only way to learn which server clock
            the batch actually used.
        applied:
          type: integer
          minimum: 0
          description: Count of operations that succeeded. Stated so a client can act on the summary without walking `results`.
        failed: { type: integer, minimum: 0 }
        results:
          type: array
          description: One entry per requested operation, **in request order**, success or failure.
          items: { $ref: '#/components/schemas/DeltaResult' }

    DeltaResult:
      type: object
      required: [index, op, success]
      properties:
        index:
          type: integer
          minimum: 0
          description: Zero-based position of this operation in the request.
        clientRef:
          oneOf: [{ type: string }, { type: 'null' }]
          description: Your own token, echoed verbatim. Null when you set none.
        op: { type: string }
        success: { type: boolean }
        errorCode:
          oneOf: [{ type: string }, { type: 'null' }]
          description: |
            The error code on failure, null on success. Note the key is **`errorCode`** here, inside
            a per-operation result — the top-level *error body* uses `code`.
        message:
          oneOf: [{ type: string }, { type: 'null' }]
          description: Human-readable, from a Custom Label. Never parsed.
        ciId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `CI__c` this operation created or acted on.
        assemblyCiId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `Assembly_CI__c` edge this operation created or acted on.
        forked:
          oneOf: [{ type: boolean }, { type: 'null' }]
          description: |
            True when an `update` had to allocate a **new** `CI_Rev__c` rather than edit one in
            place, because a baseline had frozen it. Surfaced because your next read will show a
            revision number you did not ask for.

            **Deliberately not defaulted.** Only `update` forks. `null` means *not applicable*
            (there was nothing to fork on a `claim` or an `exclude`); `false` means *this write could
            have forked and did not*. A client must treat them differently, so the field must be able
            to say both.

    # -------------------------------------------------- baselines

    BaselineCutRequest:
      type: object
      properties:
        label:
          type: string
          maxLength: 80
          description: '`Baseline__c.Name`. Over 80 characters is a 400, not a 500.'
          examples: ['Pilot build 1']
        cutDateTime:
          type: string
          format: date-time
          description: |
            Optional; defaults to the server clock. Becomes `resolutionTimePoint` in the response.
            **Never accepted in the future** (`ASOF_IN_FUTURE`) — a baseline of content that does not
            exist yet is not a baseline.

            The request keeps MVP-30030's caller-facing name; the response returns the two timestamps
            that name splits into.

    Baseline:
      type: object
      required: [baselineId, resolutionTimePoint, takenAt, memberCount]
      properties:
        baselineId: { $ref: '#/components/schemas/SalesforceId' }
        label: { type: string }
        resolutionTimePoint:
          type: string
          format: date-time
          description: What the baseline resolves against. **Stamped once, never re-derived.**
        takenAt:
          type: string
          format: date-time
          description: The wall-clock moment of the cut. Kept apart from `resolutionTimePoint` so a clock correction cannot change what a baseline resolves to.
        memberCount:
          type: integer
          minimum: 0
          description: |
            How many rows the cut resolved, **excluding tombstones**. This is the count of what the
            configuration *contains*, not the count of rows the tree endpoint renders. Also the
            canary behind `BASELINE_COUNT_DRIFT`; `BASELINE_PROVENANCE_DRIFT` is fed by the
            claimed-row count recorded alongside it.

    BaselineTree:
      type: object
      required: [baselineId, resolutionTimePoint, rows, warnings]
      properties:
        baselineId: { $ref: '#/components/schemas/SalesforceId' }
        label: { type: string }
        resolutionTimePoint:
          type: string
          format: date-time
          description: What the baseline resolved against when it was cut.
        rows:
          type: array
          description: |
            The re-resolved rows, reusing the tree shape so clients render both the same way — with
            `edgeId`, `edgeKey` and `governingTimestamp` suppressed, and excluded rows filtered out.
          items: { $ref: '#/components/schemas/TreeRow' }
        warnings:
          type: array
          description: |
            **Drift only** — `BASELINE_COUNT_DRIFT` and `BASELINE_PROVENANCE_DRIFT`. Mixed-revision
            and collision warnings from the underlying resolution are not forwarded.
          items: { $ref: '#/components/schemas/TreeWarning' }

    # -------------------------------------------------- pull

    EntryKind:
      type: string
      description: |
        What kind of change the diff found. Deliberately framed in generic framework terms (source /
        member / override) rather than MBOM terms, so the taxonomy survives a second
        configuration-item type. See the preview endpoint's table for the full catalog.
      enum:
        - source-version-advance
        - member-version-advance
        - member-added
        - member-removed
        - override-retained
        - override-superseded
        - override-orphaned
        - member-add-collision
        - member-add-converged

    Decision:
      type: string
      description: |
        Whether this entry needs the caller to decide anything. **Only two values** — there is no
        third, optional-but-defaultable tier. The only decision point anywhere in a pull is disposing
        an orphaned override; everything else, including new, removed and colliding upstream content,
        applies automatically.
      enum: [informational, required]

    Action:
      type: string
      description: |
        An action the commit endpoint can apply. Both values only ever apply to `override-orphaned`;
        a decision naming any other kind is rejected with `INVALID_REQUEST`.

        - `convert-to-native` — sever the override's provenance link, keeping its branch-owned
          content, which then resolves on the branch clock. ("Native" is the framework's existing
          term for branch-owned content with no upstream provenance.)
        - `remove` — append a deletion marker (an exclude delta). Nothing is hard-deleted.
      enum: [convert-to-native, remove]

    PullPreview:
      type: object
      required: [sourceId, branchId, sourceType, currentSourceVersion, hasChanges, summary, entries, warnings, truncated, entriesLimit, diffFingerprint, generatedAt]
      properties:
        sourceId: { $ref: '#/components/schemas/SalesforceId' }
        branchId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The owning branch, denormalised for display.
        branchLabel:
          type: string
          description: '`Branch__c.Label__c`, or `Name` when Label is blank.'
        sourceType:
          type: string
          enum: [Ebom, Pull]
          description: Only `Ebom` is in scope for Phase 1; `Pull` (branch-to-branch) is reserved.
        currentSourceVersion: { $ref: '#/components/schemas/SourceVersionRef' }
        candidateSourceVersion:
          oneOf: [{ $ref: '#/components/schemas/SourceVersionRef' }, { type: 'null' }]
          description: Absent or null when `hasChanges` is false and no newer revision exists to preview.
        hasChanges:
          type: boolean
          description: |
            False when the branch already resolves against the candidate, or there is no newer
            released revision. `entries` is empty in that case. **Not an error condition.**
        summary: { $ref: '#/components/schemas/DiffSummary' }
        entries:
          type: array
          description: |
            Every difference found, in a stable, deterministic order. Always present; empty rather
            than absent.
          items: { $ref: '#/components/schemas/DiffEntry' }
        warnings:
          type: array
          description: Non-blocking conditions — typically `MIXED_REVISION` about what would render *after* the pull. Always present; empty rather than absent.
          items: { $ref: '#/components/schemas/TreeWarning' }
        truncated:
          type: boolean
          description: |
            True when the **informational** entry set exceeded `entriesLimit` and was trimmed.
            Entries with `decision: required` are never truncated. `summary` still reflects the
            untrimmed totals.
        entriesLimit:
          type: integer
          description: The effective cap applied this call — the query param, or the server default of 200.
          examples: [200]
        diffFingerprint:
          type: string
          description: |
            Opaque `sha256:`-prefixed digest of the inputs and the **complete** resolved entry set
            (not the truncated page). Echo it back on commit; a mismatch means the world moved in
            between and fails fast with `STALE_REVISION`.

            It covers a **clock-free** projection of the entries, because a claimed row's
            `governingTimestamp` *is* the read clock — folding it in would make every commit stale on
            arrival.
          examples: ['sha256:9f2c41ab7e08d3b6']
        generatedAt:
          type: string
          format: date-time
          description: Server clock at computation time. Informational; **not** a resolution input, and not part of the fingerprint.

    SourceVersionRef:
      type: object
      description: |
        One version of a source — the accepted eBOM root revision plus the clock that governs
        traversal beneath it.

        > **Known genericity tension.** `rootItemRevisionId` / `rootItemNumber` / `rootRevisionLabel`
        > are PLM-specific: they name an `Item_Revision__c`. Correct for a `Source_Type__c = 'Ebom'`
        > source, the only type in Phase 1 scope. When a second source type is implemented, make this
        > schema a `oneOf` discriminated on the `sourceType` already present on `PullPreview` — an
        > additive change, not a rewrite.
      required: [rootItemRevisionId, resolutionTimestamp]
      properties:
        sourceRevisionId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `Branch_Source_Rev__c` recording this acceptance. **Null on `candidateSourceVersion`**, which does not exist until a commit creates it.
        rootItemRevisionId: { $ref: '#/components/schemas/SalesforceId' }
        rootItemNumber: { type: string, examples: ['Assy-100'] }
        rootRevisionLabel: { type: string, examples: ['rev-1'] }
        resolutionTimestamp:
          type: string
          format: date-time
          description: |
            **T_R** — the timestamp the eBOM traversal resolves against. Stamped once at acceptance
            from the root revision's release date and read thereafter, never re-derived. Deriving it
            per-read would let a later corrected or backdated approval silently rewrite history.
        acceptedAt:
          oneOf: [{ type: string, format: date-time }, { type: 'null' }]
          description: Wall-clock moment the acceptance was committed, kept distinct from `resolutionTimestamp`. Null on `candidateSourceVersion`.

    DiffSummary:
      type: object
      description: Counts over the **untruncated** entry set, so a caller can render "N informational, M requiring a decision" without walking `entries`.
      required: [total, informational, required, byKind]
      properties:
        total: { type: integer, minimum: 0 }
        informational: { type: integer, minimum: 0 }
        required:
          type: integer
          minimum: 0
          description: Entries that will block a commit until given an explicit decision. Only `override-orphaned` counts here.
        byKind:
          type: object
          description: Count per `EntryKind`, keyed by kind. Kinds with no entries are omitted.
          additionalProperties: { type: integer, minimum: 0 }

    DiffEntry:
      type: object
      required: [entryId, kind, decision, availableActions, target]
      properties:
        entryId:
          type: string
          description: |
            Stable, opaque handle, passed back to the commit endpoint to attach a decision. A
            **deterministic hash of `kind` plus the correlation key** — never a database Id, never
            positional, never generated — so recomputing the same diff yields the same ids. Treat it
            as opaque; do not parse it.
          examples: ['e3f19a4c8b2d6071']
        kind: { $ref: '#/components/schemas/EntryKind' }
        decision: { $ref: '#/components/schemas/Decision' }
        availableActions:
          type: array
          description: Empty except on `override-orphaned`, whose two dispositions are the only actions this API offers anywhere.
          items: { $ref: '#/components/schemas/Action' }
        retargeted:
          oneOf: [{ type: boolean }, { type: 'null' }]
          description: |
            **Present only on `override-retained`; `null` on every other kind** (deliberately not
            defaulted — a `false` would assert something about a kind for which the question is
            meaningless).

            True when the suppressed upstream change was a **re-target**: the candidate would resolve
            this edge to a different child *revision* than the override pins. False when only field
            values (quantity, find number) were suppressed. Lets a caller filter for the
            higher-stakes case — a branch silently keeping a superseded revision after upstream moved
            on — without parsing `message`.

            The child *revision*, not the child *item*: see the endpoint description.
        overrideKind:
          oneOf: [{ type: string, enum: [claim, replace] }, { type: 'null' }]
          description: |
            Present on `override-*` kinds; null elsewhere. Distinguishes an override created by
            **claiming** an upstream edge from one created by **replacing** it. Both dispose
            identically; this exists so the UI can explain what is being orphaned.
        message:
          type: string
          description: Human-readable, already-localized summary. Never parse it — switch on `kind`.
        target: { $ref: '#/components/schemas/MemberTarget' }
        current:
          oneOf: [{ $ref: '#/components/schemas/MemberState' }, { type: 'null' }]
          description: How the branch renders this member today. **Null for `member-added`.**
        candidate:
          oneOf: [{ $ref: '#/components/schemas/MemberState' }, { type: 'null' }]
          description: How it would render after the pull. **Null for `member-removed`**, and for an `override-orphaned` resolved by `remove`.

    MemberTarget:
      type: object
      description: Identifies what the entry is about, independent of before/after state.
      required: [ciKind, label]
      properties:
        ciKind: { $ref: '#/components/schemas/CiKind' }
        edgeId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The `Assembly_CI__c` for this member. Null for purely inherited content, which has no branch-owned record until something claims it.
        edgeKey:
          oneOf: [{ type: string }, { type: 'null' }]
          description: |
            The **display** key — `Assembly_CI__c.Edge_Key__c` / the legacy `Assembly__c` line id.
            **Not** what a pull correlates on: it moves when a parent item revs, which is exactly why
            the diff cannot join on it.
        label:
          type: string
          description: |
            Short display label for the member — the **child item's** item number
            (`Item__c.Name`). On a `source` target it is the root item's number instead, because
            that entry is about the root item and has no child.
          examples: ['Motor-002']
        path:
          type: array
          description: Breadcrumb from the root to this member's parent, for UI context.
          items: { $ref: '#/components/schemas/PathSegment' }

    PathSegment:
      type: object
      description: One ancestor in a breadcrumb. Same shape as `MemberTarget` minus the path — a segment never nests its own breadcrumb.
      required: [ciKind, label]
      properties:
        ciKind: { $ref: '#/components/schemas/CiKind' }
        label: { type: string, examples: ['Assy-100'] }
        edgeId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
        edgeKey:
          oneOf: [{ type: string }, { type: 'null' }]

    MemberState:
      type: object
      description: A member as resolved at one point in the diff (before or after). The generic fields are framework-level; everything type-specific lives in `detail`.
      required: [provenance, governingClock, detail]
      properties:
        provenance: { $ref: '#/components/schemas/Provenance' }
        governingClock: { $ref: '#/components/schemas/GoverningClock' }
        governingTimestamp:
          type: string
          format: date-time
          description: The concrete timestamp `governingClock` resolved to.
        detail:
          oneOf: [{ $ref: '#/components/schemas/AssemblyEdgeDetail' }]
          discriminator:
            propertyName: ciKind
            mapping:
              assembly-edge: '#/components/schemas/AssemblyEdgeDetail'

    AssemblyEdgeDetail:
      type: object
      description: |
        **The one MBOM-specific schema.** Everything above this point is reusable framework
        vocabulary; this is the single place assembly/BOM concepts appear. A second
        configuration-item type becomes a sibling schema in `MemberState.detail`'s `oneOf` and
        changes nothing else.
      required: [ciKind]
      properties:
        ciKind: { type: string, const: assembly-edge }
        parentItemId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: |
            The owning **item**, not its revision. `Assembly_CI__c` carries no parent-revision lookup
            at all: a local edge's parent item is stable across revision advances while its parent
            revision is not — and `Edge_Key__c` is unique org-wide, so two branches claiming the same
            eBOM line share one row, meaning every field on it must be branch-invariant.
        parentItemNumber: { type: string, examples: ['Assy-100'] }
        childItemId: { $ref: '#/components/schemas/SalesforceId' }
        childItemNumber: { type: string, examples: ['Motor-002'] }
        childItemRevisionId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The child revision this edge resolves to at `governingTimestamp`. Null on a tombstone.
        childRevisionLabel: { type: string, examples: ['rev-1'] }
        quantity: { type: number, examples: [4] }
        findNumber:
          type: number
          description: A Salesforce `Number` field, never a string.
          examples: [10]

    PullCommitRequest:
      type: object
      required: [candidateRootRevisionId, diffFingerprint]
      properties:
        candidateRootRevisionId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The released root revision being accepted. Same validation as the preview's query param — and re-validated here, from the re-resolved diff.
        diffFingerprint:
          type: string
          description: Echoed back from the preview this commit is acting on. A mismatch against the freshly re-resolved diff fails the whole commit with `STALE_REVISION`.
          examples: ['sha256:9f2c41ab7e08d3b6']
        decisions:
          type: array
          description: |
            One entry per `override-orphaned` entry from the preview. **Every one must appear**, or
            the commit is rejected with `DISPOSITION_REQUIRED`; naming any other kind is rejected
            with `INVALID_REQUEST`. Send `[]` (or omit) when the preview had no required entries.
          items:
            type: object
            required: [entryId, action]
            properties:
              entryId:
                type: string
                description: An `entryId` from the preview this commit is acting on.
              action: { $ref: '#/components/schemas/Action' }

    PullCommitResult:
      type: object
      required: [sourceId, newSourceRevisionId, acceptedRootRevisionId, acceptedAt, applied]
      properties:
        sourceId: { $ref: '#/components/schemas/SalesforceId' }
        newSourceRevisionId:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The `Branch_Source_Rev__c` this commit created — an **insert**, never an update.
        acceptedRootRevisionId: { $ref: '#/components/schemas/SalesforceId' }
        acceptedAt: { type: string, format: date-time }
        applied:
          type: array
          description: |
            Every entry that took an explicit decision, plus every `override-retained`,
            `override-superseded` and `member-add-converged` outcome — the set a caller needs to
            reconcile its held preview against what actually happened. (Re-previewing immediately
            after a commit is always `hasChanges: false`, so this is the only place to learn it.)

            `member-add-converged` reports `action: "retired"`; see the endpoint description.
          items: { $ref: '#/components/schemas/AppliedEntry' }

    AppliedEntry:
      type: object
      description: |
        One outcome the commit is reporting back. Four kinds appear here and no others:
        `override-orphaned` (the decision you supplied), `override-retained` and
        `override-superseded` (informational), and `member-add-converged` (retired by the commit).

        A re-preview straight after a commit is always `hasChanges: false`, so this array is the
        only place the retained and superseded outcomes can be read.
      required: [entryId, kind]
      properties:
        entryId: { type: string }
        kind:
          type: string
          enum: [override-orphaned, override-retained, override-superseded, member-add-converged]
        action:
          oneOf: [{ type: string }, { type: 'null' }]
          description: |
            What was done:

            - `convert-to-native` or `remove` — the decision supplied for an `override-orphaned`
              entry.
            - `retired` — on `member-add-converged`. The commit appended a `Revert` delta dropping
              the branch's now-redundant local assertion. No decision is accepted for this kind.
            - `null` — on `override-retained` and `override-superseded`, where nothing was written.
        edgeId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The branch-owned `Assembly_CI__c` this entry's target names, if any.

    # -------------------------------------------------- comparison

    CompareSideType:
      type: string
      description: |
        Which resolution produced a side of a comparison. The right-hand side may be any of the
        four; the **left** may only be `branch` or `baseline`, because those are the two objects a
        comparison path is rooted at.

        Sent as `rightType` — where it is matched case-insensitively — and returned as
        `left.type` / `right.type`, always in this lower-case spelling.
      enum: [branch, baseline, source, ebom]

    CompareKind:
      type: string
      description: |
        What a comparison entry reports. The counterpart to `EntryKind`, which is the **pull's**
        vocabulary, and deliberately a separate set: a pull is asymmetric and its kinds name what a
        pull would *do*, while a comparison is symmetric and does nothing. `member-added` and
        `member-removed` are the two wire values the two vocabularies share, and they mean the same
        thing in both.

        See the endpoint description for what each one means and when it is emitted.
      enum:
        - root-version-differs
        - member-added
        - member-removed
        - member-differs
        - member-unchanged

    Comparison:
      type: object
      description: |
        The whole answer to "how do these two configurations differ": what each side was, one entry
        per differing correlation key, counts over the complete set, and each side's own warnings.

        No envelope, nulls not suppressed, and every collection always `[]` rather than absent —
        the same conventions as the rest of this API.
      required: [left, right, identical, summary, entries, leftWarnings, rightWarnings, truncated, entriesLimit, generatedAt]
      properties:
        left:
          allOf: [{ $ref: '#/components/schemas/ComparisonSide' }]
          description: The side the path named — a branch or a baseline.
        right:
          allOf: [{ $ref: '#/components/schemas/ComparisonSide' }]
          description: The side `rightType` / `rightId` named.
        identical:
          type: boolean
          description: |
            True when the two configurations contain the same members with the same content.

            Derived from the entry list rather than from the summary: true exactly when no entry of
            any kind other than `member-unchanged` is present. So it reads the same whether or not
            the unchanged rows were asked for, and a `root-version-differs` entry is enough on its
            own to make it false.
        summary: { $ref: '#/components/schemas/CompareSummary' }
        entries:
          type: array
          description: |
            One entry per differing correlation key — plus every matching one when
            `includeUnchanged=true` — ordered by breadcrumb path, then find number, then child item
            id, then kind. Always present; empty rather than absent, and empty is the ordinary
            shape of an identical comparison.
          items: { $ref: '#/components/schemas/CompareEntry' }
        leftWarnings:
          type: array
          description: |
            Warnings about the **left** side's own resolution — typically `MIXED_REVISION` — plus
            `COMPARE_ROOTS_DIFFER`, which is about the pair and is reported here, first, because it
            reframes everything below it.

            Split per side rather than by adding a `side` field to `TreeWarning`, which would put a
            new null key on every existing tree endpoint's warnings.
          items: { $ref: '#/components/schemas/TreeWarning' }
        rightWarnings:
          type: array
          description: Warnings about the **right** side's own resolution. Always present; empty rather than absent.
          items: { $ref: '#/components/schemas/TreeWarning' }
        truncated:
          type: boolean
          description: |
            True when `entries` was capped at `entriesLimit`. The cap is a plain prefix and
            `summary` still reports untruncated totals, which is what makes this actionable.
        entriesLimit:
          type: integer
          description: The effective cap applied this call — the query parameter, or the server default of 200.
          examples: [200]
        generatedAt:
          type: string
          format: date-time
          description: |
            Server clock at computation time, read **once** for the whole call so the two sides are
            never resolved microseconds apart. Not a resolution input — each side publishes its own
            clocks.

    ComparisonSide:
      type: object
      description: |
        One of the two configurations that were compared: what it was, how it resolved, and on
        which clock.

        Published for **both** sides rather than echoed only for the one the path named, because a
        comparison's answer is unreadable without knowing what was compared — `member-added` means
        nothing until the reader knows the right side is a baseline from March rather than the
        branch's own source.
      required: [type, id]
      properties:
        type: { $ref: '#/components/schemas/CompareSideType' }
        id:
          allOf: [{ $ref: '#/components/schemas/SalesforceId' }]
          description: The record this side was addressed by — a `Branch__c`, `Baseline__c`, `Branch_Source__c` or `Item_Revision__c`.
        label:
          oneOf: [{ type: string }, { type: 'null' }]
          description: |
            The side's own display name where the addressed record has one — `Branch__c.Label__c`
            for a branch, `Baseline__c.Name` for a baseline — and the **root item number** otherwise,
            so a UI always has something to render beside the column. A `source` and an `ebom` side
            are named by what they supply rather than by a record a user would recognise.
          examples: ['MBOM-GDL']
        rootItemId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
        rootItemNumber:
          oneOf: [{ type: string }, { type: 'null' }]
          examples: ['Assy-100']
        rootRevisionId:
          oneOf: [{ $ref: '#/components/schemas/SalesforceId' }, { type: 'null' }]
          description: The root `Item_Revision__c` this side resolved to. **Null** when a source has nothing accepted yet.
        rootRevisionLabel:
          oneOf: [{ type: string }, { type: 'null' }]
          examples: ['rev-1']
        asOf:
          type: string
          format: date-time
          description: |
            The read clock this side was resolved at — the caller's `asOf` / `rightAsOf`, the
            server clock when none was supplied, an eBOM revision's release date, or a baseline's
            frozen `Resolution_TimePoint__c`. A baseline echoes the timepoint its **cut** recorded,
            which makes it obvious the caller could not have chosen it.
        sourceTimePoint:
          oneOf: [{ type: string, format: date-time }, { type: 'null' }]
          description: |
            **T_R** — the timepoint this side's *inherited* content is pinned at. Separate from
            `asOf` because that is the whole model: a side reporting one number could not explain
            its own rows.

            An `ebom` side publishes the same value in both, honestly rather than leaving this null:
            a raw walk has no acceptance pinning it and no branch content floating above it, so its
            traversal clock **is** its T_R. Null only where a source has accepted nothing.

    CompareEntry:
      type: object
      description: |
        One difference between the two configurations, keyed by correlation key.

        **Not the pull's `DiffEntry`.** That contract names its two states `current` and
        `candidate`, which is a lie on a symmetric pair — a baseline compared against a raw eBOM has
        no "current" side — and it carries four fields a comparison has no use for. The **leaf**
        types are shared deliberately: `MemberTarget`, `MemberState`, `PathSegment` and
        `AssemblyEdgeDetail` describe where a member sits and how one side renders it, which is
        identical work in both features. Only the envelope differs.
      required: [entryId, kind, differingFields, message, target]
      properties:
        entryId:
          type: string
          description: |
            Stable, opaque handle: a deterministic hash of the kind plus the correlation key,
            truncated to 16 hex characters. Never a database id, never positional, never generated —
            so a repeated comparison returns the same handles. There is nothing to pass it back to;
            it exists so a client can key a row. Do not parse it.

            **One entry per correlation key, always** — two entries on one key would collide, which
            is why `member-differs` carries `differingFields` rather than splitting into one kind
            per field.
          examples: ['7b31c0d5e9a24f18']
        kind: { $ref: '#/components/schemas/CompareKind' }
        differingFields:
          type: array
          description: |
            Which of the three assertable values disagree, in a fixed order so two calls produce
            byte-identical lists. Populated on `member-differs`; **empty on every other kind** —
            empty rather than null, so a client can iterate unconditionally.

            Provenance is deliberately not among them: each side's state already publishes it, and
            a provenance-only difference is not a content difference.
          items:
            type: string
            enum: [quantity, findNumber, childRevision]
        message:
          type: string
          description: Human-readable, from a Custom Label, with the member's own item number appended. Translated; switch on `kind`, never on text.
        target: { $ref: '#/components/schemas/MemberTarget' }
        left:
          oneOf: [{ $ref: '#/components/schemas/MemberState' }, { type: 'null' }]
          description: How the **left** side renders this member. **Null on `member-added`.**
        right:
          oneOf: [{ $ref: '#/components/schemas/MemberState' }, { type: 'null' }]
          description: How the **right** side renders this member. **Null on `member-removed`.**

    CompareSummary:
      type: object
      description: |
        Counts over the **whole** comparison, never over a truncated entry list.

        Read the two groups differently. `total` and `byKind` count entries **emitted**, so both
        move with `includeUnchanged`. `added`, `removed`, `differing` and `unchanged` count
        correlation keys and do not.
      required: [total, added, removed, differing, unchanged, byKind]
      properties:
        total:
          type: integer
          minimum: 0
          description: Entries produced — `root-version-differs` and any `member-unchanged` included. Moves with `includeUnchanged`.
        added:
          type: integer
          minimum: 0
          description: Correlation keys present on the **right** side only.
        removed:
          type: integer
          minimum: 0
          description: Correlation keys present on the **left** side only.
        differing:
          type: integer
          minimum: 0
          description: Correlation keys present on both sides whose content disagrees.
        unchanged:
          type: integer
          minimum: 0
          description: |
            Correlation keys present on both sides and identical.

            **Populated whether or not `includeUnchanged` was set**, which is the point of it: the
            default comparison emits no row for an identical member, and a caller still needs to
            know how much of the configuration matched without paying for the rows. It therefore
            does **not** always equal `byKind['member-unchanged']`, which counts emitted entries and
            is `0` by default.
        byKind:
          type: object
          description: |
            Count per `CompareKind`, keyed by its wire value, over emitted entries.

            **Pre-seeded with all five kinds at zero**, unlike the pull's `DiffSummary`, which
            carries only the kinds it observed: a comparison is read by a client rendering a fixed
            set of counters, and an absent key would make each one a null check.
          additionalProperties: { type: integer, minimum: 0 }

    # -------------------------------------------------- errors

    RestError:
      type: object
      description: |
        The MBOM's error shape. Keyed **`code`**, not the framework's `errorCode`.

        A 403 body carries the label **only**: naming the object a caller was just denied would hand
        them the schema they have no access to. It goes to the debug log instead. A 500 *does* carry
        the exception type and message — an unmodelled fault has to be diagnosable and its caller is
        our own UI or an integration — but never the stack trace.
      required: [code, message]
      properties:
        code:
          type: string
          description: |
            The stable error code. Every value below is a client error; a 500 carries
            `INTERNAL_ERROR`, which is listed here for completeness but is not a code to branch on.
          enum:
            [CLAIM_REQUIRED, ALREADY_CLAIMED, ALREADY_EXCLUDED, NOT_SUPPLIED, NO_OVERRIDE,
             HARD_DELETE_BLOCKED, IMMUTABLE_FIELD, NOT_RELEASED, STALE_REVISION, ALREADY_PRESENT,
             CANDIDATE_NOT_NEWER, DISPOSITION_REQUIRED, CYCLE_DETECTED, EBOM_CYCLE,
             PARENT_NOT_IN_BRANCH, UNSUPPORTED_CI_TYPE, CI_IDENTITY_UNRESOLVED,
             CI_IDENTITY_COLLISION, PROVENANCE_INVALID, CANDIDATE_ROOT_MISMATCH,
             AMBIGUOUS_CORRELATION, BATCH_TOO_LARGE, ASOF_IN_FUTURE, ASOF_INVALID, NOT_EDITABLE,
             INVALID_REQUEST, NOT_FOUND, FORBIDDEN, INTERNAL_ERROR]
          examples: ['NOT_RELEASED']
        message:
          type: string
          description: |
            The Custom Label for `code`, with a parenthesised **detail** appended naming the specific
            row or value. The label says what went wrong and what to do about it; the detail says
            which row it happened to. Translated — never parse it.
        failures:
          type: array
          description: |
            Present **only** on an atomic delta batch that failed validation. Carries the complete
            per-operation failure list, so a caller does not have to find their faults one round trip
            at a time.
          items: { $ref: '#/components/schemas/DeltaResult' }

  # ---------------------------------------------------------------- shared responses

  responses:

    Forbidden:
      description: |
        `FORBIDDEN` (403). The caller lacks an object grant this endpoint requires. The body names no
        object — see `RestError`.

        A field-level denial deeper in the stack is also answered 403: under `USER_MODE` that
        surfaces as a `QueryException`, and every query on this path is built from a fixed field
        list, so an access failure is far and away the likeliest cause.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            denied:
              value:
                code: FORBIDDEN
                message: 'You do not have permission to perform this action.'

    NotFoundPath:
      description: |
        `NOT_FOUND` (404). The path id is absent, unparseable, or names a **different object** than
        this endpoint expects — the last of which is why swapping a `branchId` for a `sourceId` is a
        clean 404 rather than a 500 out of a repository.

        Also answered for any unrecognised trailing segment: `GET .../branches/{id}/bogus` is the
        MBOM's own 404 in the MBOM's own shape (`code`), not the framework's (`errorCode`).
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            wrongObject:
              value:
                code: NOT_FOUND
                message: 'That record does not exist, or you do not have access to it. (a1Cg8000000AbCeEAK is not a Branch__c)'

    PullConflictPreview:
      description: |
        409 — well formed, retryable after state changes.

        | code | Cause |
        |---|---|
        | `NOT_RELEASED` | the candidate is not Released, or is Cancelled, Expired, or missing a release date |
        | `CANDIDATE_NOT_NEWER` | the candidate resolves at or before the source's current T_R |
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            notNewer:
              value:
                code: CANDIDATE_NOT_NEWER
                message: 'That revision is not newer than what this branch already resolves against. Choose a later release, or none is available yet. (a0Ag8000000Ab01EAC)'

    PullConflictCommit:
      description: |
        409 — well formed, retryable after state changes.

        | code | Cause | Remedy |
        |---|---|---|
        | `STALE_REVISION` | `diffFingerprint` mismatch, or an `entryId` that no longer names an entry in the freshly re-resolved diff | re-run `pull-preview` and resubmit |
        | `DISPOSITION_REQUIRED` | an `override-orphaned` entry has no decision | supply one for every required entry |
        | `NOT_RELEASED` / `CANDIDATE_NOT_NEWER` | revalidated at commit time and no longer qualifies | pick another candidate |
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            stale:
              value:
                code: STALE_REVISION
                message: 'This row has been revised since the view you are working from. Reload it and try again. (diffFingerprint sha256:9f2c41ab7e08d3b6 no longer matches; re-run pull-preview and resubmit)'
            dispositionRequired:
              value:
                code: DISPOSITION_REQUIRED
                message: 'This pull orphans an override you made. Choose to keep it as branch-owned content or remove it before committing. (f4880c6b3e1a9d75, Harness-011)'

    PullUnprocessable:
      description: |
        422 — well formed, but impossible against the model. Retrying the identical request will
        never succeed.

        | code | Cause |
        |---|---|
        | `CANDIDATE_ROOT_MISMATCH` | the candidate belongs to a **different root item** than the source |
        | `CYCLE_DETECTED` / `EBOM_CYCLE` | a cycle in the eBOM below the candidate root |
        | `AMBIGUOUS_CORRELATION` | two edges on the **same side** of the diff share a correlation key, so which upstream line matches which branch line cannot be determined — no branch state the caller can reach will fix it |
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            rootMismatch:
              value:
                code: CANDIDATE_ROOT_MISMATCH
                message: "That revision belongs to a different item than this branch's source. Choose a revision of the same root item. (a0Ag8000000Ab77EAC)"
            cycle:
              value:
                code: CYCLE_DETECTED
                message: 'This change would make the child an ancestor of its own parent, which no BOM can represent. (beneath Assy-100 rev-2 at edge Harness-011)'

    ComparisonConflict:
      description: |
        409 — well formed, and retryable once state changes.

        | code | Cause |
        |---|---|
        | `NOT_RELEASED` | `rightType=ebom` names a revision that is not Released, or is Cancelled, Expired, or carries no release date |

        The same four-part test an MBOM's creation and a pull both apply, and a 409 for the same
        reason: releasing the revision makes the identical request succeed. A Draft revision never
        appears at any timepoint and has no release date to pin a walk to, so there is nothing to
        compare against.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            notReleased:
              value:
                code: NOT_RELEASED
                message: 'An mBOM can only be created from a released item revision. (a0Ag8000000Ab99EAC)'

    ComparisonUnprocessable:
      description: |
        422 — well formed, but impossible against the model. Retrying the identical request will
        never succeed.

        | code | Cause |
        |---|---|
        | `AMBIGUOUS_CORRELATION` | two edges on the **same side** share a correlation key, so which member on one side matches which on the other cannot be determined. No state the caller can reach will fix it — the same known gap the pull has |
        | `CYCLE_DETECTED` | a cycle in the eBOM beneath either side's root, so that side cannot be resolved at all |

        Both sides are resolved, so either can raise either code. `AMBIGUOUS_CORRELATION`'s message
        names the offending side in the **join's** vocabulary rather than the comparison's:
        `current side` is the **left** side and `candidate side` is the **right** one. The message
        is shared with pull-preview, which is the surface that vocabulary was written for; switch on
        `code`, and read the ids rather than the nouns.

        A comparison always reports a cycle as **`CYCLE_DETECTED`**, never as the traversal's own
        `EBOM_CYCLE`: the two mean the same thing to a caller and are both 422, and carrying the
        inner code out would make this contract depend on which internal walk happened to find it.
        A tree read, which has one walk and no such choice, still answers `EBOM_CYCLE`.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            ambiguous:
              value:
                code: AMBIGUOUS_CORRELATION
                message: 'Two BOM lines share the same parent, child and secondary unique key, so this pull cannot tell which line corresponds to which. If these are genuinely duplicate lines, give one its own secondary unique key. If the parent item legitimately renders at two revisions in this tree, this is a known limitation (MVP-30214), not a data error -- contact support rather than editing BOM data that looks correct. (current side: parent item a02g8000000Ab01EAC, child item a02g8000000Ab20EAC, secondary unique key (none); edges a0Bg8000000Ab10EAC and a0Bg8000000Ab19EAC)'
            cycle:
              value:
                code: CYCLE_DETECTED
                message: 'This change would make the child an ancestor of its own parent, which no BOM can represent. (beneath Assy-100 rev-2 at edge Harness-011)'

    InternalError:
      description: |
        500. Unhandled server-side failure. The body carries `INTERNAL_ERROR` plus the exception
        type and message, and never the stack trace. Never answered as a 200.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/RestError' }
          examples:
            unexpected:
              value:
                code: INTERNAL_ERROR
                message: 'System.NullPointerException: Attempt to de-reference a null object'

  # ---------------------------------------------------------------- examples

  examples:

    BranchTreeFlat:
      value:
        asOf: '2026-04-05T17:22:03.518Z'
        rootRevisionId: a0Ag8000000Ab01EAC
        sourceTimePoint: '2026-01-15T00:00:00.000Z'
        rows:
          - edgeId: a1Eg8000000AbD1EAK
            edgeKey: a0Bg8000000Ab10EAC
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab20EAC
              childItemRevisionId: a0Ag8000000Ab41EAC
              quantity: 4
              findNumber: 10
            provenance: claimed
            governingTimestamp: '2026-01-15T00:00:00.000Z'
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: true
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows: []
          - edgeId: null
            edgeKey: a0Bg8000000Ab11EAC
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab21EAC
              childItemRevisionId: a0Ag8000000Ab31EAC
              quantity: 2
              findNumber: 20
            provenance: inherited
            governingTimestamp: '2026-01-15T00:00:00.000Z'
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: false
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows: []
          - edgeId: a1Eg8000000AbD3EAK
            edgeKey: a0Bg8000000Ab12EAC
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab22EAC
              childItemRevisionId: a0Ag8000000Ab61EAC
              quantity: 3
              findNumber: 30
            provenance: claimed
            governingTimestamp: '2026-01-15T00:00:00.000Z'
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: true
            isExcluded: true
            isReplaced: false
            isEditable: true
            childRows: []
          - edgeId: a1Eg8000000AbD9EAK
            edgeKey: 'local-a1Bg8000000AbCdEAK-a02g8000000Ab01EAC-a02g8000000Ab90EAC'
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab90EAC
              childItemRevisionId: a0Ag8000000Ab95EAC
              quantity: 1
              findNumber: 90
            provenance: local
            governingTimestamp: '2026-04-05T17:22:03.518Z'
            sourceId: null
            isClaimed: false
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows: []
        warnings: []

    BranchTreeNested:
      value:
        asOf: '2026-04-05T17:22:03.518Z'
        rootRevisionId: a0Ag8000000Ab01EAC
        sourceTimePoint: '2026-01-15T00:00:00.000Z'
        rows:
          - edgeId: null
            edgeKey: a0Bg8000000Ab14EAC
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab30EAC
              childItemId: a02g8000000Ab31EAC
              childItemRevisionId: a0Ag8000000Ab72EAC
              quantity: 4
              findNumber: 10
            provenance: inherited
            governingTimestamp: '2026-01-15T00:00:00.000Z'
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: false
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows:
              - edgeId: null
                edgeKey: a0Bg8000000Ab15EAC
                provenance: inherited
                governingTimestamp: '2026-01-15T00:00:00.000Z'
                sourceId: a1Cg8000000AbCeEAK
                isClaimed: false
                isExcluded: false
                isReplaced: false
                isEditable: true
                childRows: []
        warnings: []

    BranchTreeMixedRevision:
      value:
        asOf: '2026-04-05T17:22:03.518Z'
        rootRevisionId: a0Ag8000000Ab01EAC
        sourceTimePoint: '2026-01-15T00:00:00.000Z'
        rows: []
        warnings:
          - warningCode: MIXED_REVISION
            message: 'This item appears at more than one revision in the same tree, because inherited rows are pinned to the source timepoint while claimed and local rows follow the as-of date. This is expected and does not block anything.'
            itemId: a02g8000000Ab21EAC
            itemRevisionIds: [a0Ag8000000Ab31EAC, a0Ag8000000Ab32EAC]
            governingTimestamps: ['2026-01-15T00:00:00.000Z', '2026-04-05T17:22:03.518Z']

    SourceTree:
      value:
        asOf: '2026-04-05T17:22:03.518Z'
        rootRevisionId: a0Ag8000000Ab01EAC
        sourceTimePoint: '2026-01-15T00:00:00.000Z'
        rows:
          - edgeId: null
            edgeKey: a0Bg8000000Ab10EAC
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab20EAC
              childItemRevisionId: a0Ag8000000Ab41EAC
              quantity: 1
              findNumber: 10
            provenance: inherited
            governingTimestamp: '2026-01-15T00:00:00.000Z'
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: false
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows: []
        warnings: []

    DeltaBatchApplied:
      value:
        branchId: a1Bg8000000AbCdEAK
        atomic: true
        asOf: '2026-04-05T17:22:03.518Z'
        applied: 2
        failed: 0
        results:
          - index: 0
            clientRef: row-motor
            op: claim
            success: true
            errorCode: null
            message: null
            ciId: a1Gg8000000AbF1EAK
            assemblyCiId: a1Eg8000000AbD1EAK
            forked: null
          - index: 1
            clientRef: row-bracket
            op: claim
            success: true
            errorCode: null
            message: null
            ciId: a1Gg8000000AbF2EAK
            assemblyCiId: a1Eg8000000AbD2EAK
            forked: null

    DeltaBatchPartial:
      value:
        branchId: a1Bg8000000AbCdEAK
        atomic: false
        asOf: '2026-04-05T17:25:11.004Z'
        applied: 1
        failed: 1
        results:
          - index: 0
            clientRef: ok
            op: claim
            success: true
            errorCode: null
            message: null
            ciId: a1Gg8000000AbF2EAK
            assemblyCiId: a1Eg8000000AbD2EAK
            forked: null
          - index: 1
            clientRef: will-fail
            op: update
            success: false
            errorCode: CLAIM_REQUIRED
            message: 'This row is inherited and read-only. Claim it before editing. (edge a0Bg8000000Ab12EAC)'
            ciId: null
            assemblyCiId: null
            forked: null

    DeltaBatchForked:
      value:
        branchId: a1Bg8000000AbCdEAK
        atomic: true
        asOf: '2026-04-06T15:02:00.000Z'
        applied: 1
        failed: 0
        results:
          - index: 0
            clientRef: qty-fix
            op: update
            success: true
            errorCode: null
            message: null
            ciId: a1Gg8000000AbF1EAK
            assemblyCiId: a1Eg8000000AbD1EAK
            forked: true

    DeltaBatchAtomicFailure:
      value:
        code: CLAIM_REQUIRED
        message: 'This row is inherited and read-only. Claim it before editing. (2 of 3 operations failed validation)'
        failures:
          - index: 1
            clientRef: will-fail
            op: update
            success: false
            errorCode: CLAIM_REQUIRED
            message: 'This row is inherited and read-only. Claim it before editing. (edge a0Bg8000000Ab12EAC)'
            ciId: null
            assemblyCiId: null
            forked: null
          - index: 2
            clientRef: also-fails
            op: exclude
            success: false
            errorCode: ALREADY_EXCLUDED
            message: 'This row is already excluded from this configuration. Restore it first if you want to change it. (edge a0Bg8000000Ab13EAC is already excluded)'
            ciId: null
            assemblyCiId: null
            forked: null

    DeltaBatchUnknownOp:
      value:
        code: INVALID_REQUEST
        message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (1 of 1 operations failed validation)'
        failures:
          - index: 0
            clientRef: typo
            op: claimm
            success: false
            errorCode: INVALID_REQUEST
            message: 'That request could not be read. Check that the body is valid JSON and that every required value is supplied. (unknown op claimm)'
            ciId: null
            assemblyCiId: null
            forked: null

    BaselineTreeClean:
      value:
        baselineId: a1Fg8000000AbE1EAK
        label: Pilot build 1
        resolutionTimePoint: '2026-04-06T14:31:02.000Z'
        rows:
          - edgeId: null
            edgeKey: null
            detail:
              ciKind: assembly-edge
              parentItemId: a02g8000000Ab01EAC
              childItemId: a02g8000000Ab20EAC
              childItemRevisionId: a0Ag8000000Ab41EAC
              quantity: 4
              findNumber: 10
            provenance: claimed
            governingTimestamp: null
            sourceId: a1Cg8000000AbCeEAK
            isClaimed: true
            isExcluded: false
            isReplaced: false
            isEditable: true
            childRows: []
        warnings: []

    BaselineTreeDrift:
      value:
        baselineId: a1Fg8000000AbE1EAK
        label: Pilot build 1
        resolutionTimePoint: '2026-04-06T14:31:02.000Z'
        rows: []
        warnings:
          - warningCode: BASELINE_COUNT_DRIFT
            message: 'This baseline now resolves a different number of rows than when it was cut. The stored timepoint has not moved; something it depends on has.'
            itemId: null
            itemRevisionIds: []
            governingTimestamps: []

    PullPreviewWithChanges:
      value:
        sourceId: a1Cg8000000AbCeEAK
        branchId: a1Bg8000000AbCdEAK
        branchLabel: MBOM-GDL
        sourceType: Ebom
        currentSourceVersion:
          sourceRevisionId: a1Dg8000000AbCfEAK
          rootItemRevisionId: a0Ag8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionLabel: rev-1
          resolutionTimestamp: '2026-01-15T00:00:00.000Z'
          acceptedAt: '2026-01-15T09:12:44.000Z'
        candidateSourceVersion:
          sourceRevisionId: null
          rootItemRevisionId: a0Ag8000000Ab02EAC
          rootItemNumber: Assy-100
          rootRevisionLabel: rev-2
          resolutionTimestamp: '2026-04-02T00:00:00.000Z'
          acceptedAt: null
        hasChanges: true
        summary:
          total: 6
          informational: 5
          required: 1
          byKind:
            source-version-advance: 1
            member-version-advance: 1
            override-retained: 1
            member-added: 1
            override-orphaned: 1
            member-add-converged: 1
        entries:
          - entryId: '5c1e0a77f9b34d20'
            kind: source-version-advance
            decision: informational
            availableActions: []
            retargeted: null
            overrideKind: null
            message: 'The source assembly moves to a newer released revision.'
            target:
              ciKind: source
              edgeId: null
              edgeKey: null
              label: Assy-100
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current: null
            candidate: null

          - entryId: 'a90f3b21c7d85e64'
            kind: member-version-advance
            decision: informational
            availableActions: []
            retargeted: null
            overrideKind: null
            message: 'This member resolves differently at the newer source timepoint. Nothing to decide: a revision is an outcome of the timepoint, not a value you can set.'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab11EAC
              label: Bracket-005
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab21EAC
                childItemNumber: Bracket-005
                childItemRevisionId: a0Ag8000000Ab31EAC
                childRevisionLabel: rev-3
                quantity: 2
                findNumber: 20
            candidate:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab21EAC
                childItemNumber: Bracket-005
                childItemRevisionId: a0Ag8000000Ab32EAC
                childRevisionLabel: rev-4
                quantity: 2
                findNumber: 20

          - entryId: 'e3f19a4c8b2d6071'
            kind: override-retained
            decision: informational
            availableActions: []
            retargeted: false
            overrideKind: claim
            message: "Your change to this member is kept, and the newer revision's version of it is suppressed."
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD1EAK
              edgeKey: a0Bg8000000Ab10EAC
              label: Motor-002
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current:
              provenance: claimed
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab41EAC
                childRevisionLabel: rev-1
                quantity: 4
                findNumber: 10
            candidate:
              provenance: claimed
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab41EAC
                childRevisionLabel: rev-1
                quantity: 4
                findNumber: 10

          - entryId: 'b7204de9a1c6f358'
            kind: member-added
            decision: informational
            availableActions: []
            retargeted: null
            overrideKind: null
            message: 'The newer revision adds this member. It comes in automatically.'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab13EAC
              label: Panel-300
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current: null
            candidate:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab23EAC
                childItemNumber: Panel-300
                childItemRevisionId: a0Ag8000000Ab52EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 40

          - entryId: 'f4880c6b3e1a9d75'
            kind: override-orphaned
            decision: required
            availableActions: [convert-to-native, remove]
            retargeted: null
            overrideKind: claim
            message: 'The newer revision removes a member you changed. Choose whether to keep it as your own content or remove it.'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD3EAK
              edgeKey: a0Bg8000000Ab12EAC
              label: Harness-011
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current:
              provenance: claimed
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab22EAC
                childItemNumber: Harness-011
                childItemRevisionId: a0Ag8000000Ab61EAC
                childRevisionLabel: rev-1
                quantity: 3
                findNumber: 30
            candidate: null

          - entryId: '2d6be1470f83a9c5'
            kind: member-add-converged
            decision: informational
            availableActions: []
            retargeted: null
            overrideKind: null
            message: 'The newer revision now supplies this member exactly as you added it. Your local copy is retired and the position becomes inherited content.'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD5EAK
              edgeKey: 'local-a1Bg8000000AbCdEAK-a02g8000000Ab01EAC-a02g8000000Ab24EAC'
              label: Label-400
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            current:
              provenance: local
              governingClock: branch
              governingTimestamp: '2026-04-05T17:22:03.518Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab24EAC
                childItemNumber: Label-400
                childItemRevisionId: a0Ag8000000Ab71EAC
                childRevisionLabel: rev-1
                quantity: 1
                findNumber: 60
            candidate:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab24EAC
                childItemNumber: Label-400
                childItemRevisionId: a0Ag8000000Ab71EAC
                childRevisionLabel: rev-1
                quantity: 1
                findNumber: 60
        warnings: []
        truncated: false
        entriesLimit: 200
        diffFingerprint: 'sha256:9f2c41ab7e08d3b6'
        generatedAt: '2026-04-05T17:22:03.518Z'

    PullPreviewNoChanges:
      value:
        sourceId: a1Cg8000000AbCeEAK
        branchId: a1Bg8000000AbCdEAK
        branchLabel: MBOM-GDL
        sourceType: Ebom
        currentSourceVersion:
          sourceRevisionId: a1Dg8000000AbCgEAK
          rootItemRevisionId: a0Ag8000000Ab02EAC
          rootItemNumber: Assy-100
          rootRevisionLabel: rev-2
          resolutionTimestamp: '2026-04-02T00:00:00.000Z'
          acceptedAt: '2026-04-05T17:30:11.000Z'
        candidateSourceVersion: null
        hasChanges: false
        summary:
          total: 0
          informational: 0
          required: 0
          byKind: {}
        entries: []
        warnings: []
        truncated: false
        entriesLimit: 200
        diffFingerprint: 'sha256:0000000000000000'
        generatedAt: '2026-04-06T08:00:00.000Z'

    PullPreviewTruncated:
      summary: '`entriesLimit=1` — one informational entry, and the required one kept regardless'
      value:
        sourceId: a1Cg8000000AbCeEAK
        branchId: a1Bg8000000AbCdEAK
        branchLabel: MBOM-GDL
        sourceType: Ebom
        currentSourceVersion:
          sourceRevisionId: a1Dg8000000AbCfEAK
          rootItemRevisionId: a0Ag8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionLabel: rev-1
          resolutionTimestamp: '2026-01-15T00:00:00.000Z'
          acceptedAt: '2026-01-15T09:12:44.000Z'
        candidateSourceVersion:
          sourceRevisionId: null
          rootItemRevisionId: a0Ag8000000Ab02EAC
          rootItemNumber: Assy-100
          rootRevisionLabel: rev-2
          resolutionTimestamp: '2026-04-02T00:00:00.000Z'
          acceptedAt: null
        hasChanges: true
        summary:
          total: 5
          informational: 4
          required: 1
          byKind:
            source-version-advance: 1
            member-version-advance: 1
            override-retained: 1
            member-added: 1
            override-orphaned: 1
        entries:
          - entryId: '5c1e0a77f9b34d20'
            kind: source-version-advance
            decision: informational
            availableActions: []
            message: 'The source assembly moves to a newer released revision.'
            target:
              ciKind: source
              label: Assy-100
              path: [{ ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }]
          - entryId: 'f4880c6b3e1a9d75'
            kind: override-orphaned
            decision: required
            availableActions: [convert-to-native, remove]
            overrideKind: claim
            message: 'The newer revision removes a member you changed. Choose whether to keep it as your own content or remove it.'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD3EAK
              edgeKey: a0Bg8000000Ab12EAC
              label: Harness-011
              path: [{ ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }]
        warnings: []
        truncated: true
        entriesLimit: 1
        diffFingerprint: 'sha256:9f2c41ab7e08d3b6'
        generatedAt: '2026-04-05T17:22:03.518Z'

    PullCommitCommitted:
      value:
        sourceId: a1Cg8000000AbCeEAK
        newSourceRevisionId: a1Dg8000000AbChEAK
        acceptedRootRevisionId: a0Ag8000000Ab02EAC
        acceptedAt: '2026-04-06T14:02:51.317Z'
        applied:
          - entryId: 'f4880c6b3e1a9d75'
            kind: override-orphaned
            action: convert-to-native
            edgeId: a1Eg8000000AbD3EAK
          - entryId: 'e3f19a4c8b2d6071'
            kind: override-retained
            action: null
            edgeId: a1Eg8000000AbD1EAK
          - entryId: '2d6be1470f83a9c5'
            kind: member-add-converged
            action: retired
            edgeId: a1Eg8000000AbD5EAK

    PullCommitAlreadyCurrent:
      value:
        sourceId: a1Cg8000000AbCeEAK
        newSourceRevisionId: a1Dg8000000AbCiEAK
        acceptedRootRevisionId: a0Ag8000000Ab02EAC
        acceptedAt: '2026-04-06T16:11:09.882Z'
        applied: []

    # The four comparison examples below are ONE fixture read four ways, so they can be read
    # against each other. `ComparisonWithDifferences`, `ComparisonIncludingUnchanged` and
    # `ComparisonTruncated` are the same branch against the same eBOM revision, differing only in
    # `includeUnchanged` and `entriesLimit` — which is what makes the summary's behaviour legible:
    # `unchanged` is 1 in all three, `total` and `byKind['member-unchanged']` move only with the
    # flag, and truncation moves neither.

    ComparisonIdentical:
      value:
        left:
          type: branch
          id: a1Bg8000000AbCdEAK
          label: MBOM-GDL
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab01EAC
          rootRevisionLabel: rev-1
          asOf: '2026-04-05T17:22:03.518Z'
          sourceTimePoint: '2026-01-15T00:00:00.000Z'
        right:
          type: source
          id: a1Cg8000000AbCeEAK
          label: Assy-100
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab01EAC
          rootRevisionLabel: rev-1
          asOf: '2026-04-05T17:22:03.518Z'
          sourceTimePoint: '2026-01-15T00:00:00.000Z'
        identical: true
        summary:
          total: 0
          added: 0
          removed: 0
          differing: 0
          # Four members matched and agreed. One of them is a member the branch CLAIMED without
          # changing: provenance is reported, never classified, so it is unchanged content.
          unchanged: 4
          byKind:
            root-version-differs: 0
            member-added: 0
            member-removed: 0
            member-differs: 0
            member-unchanged: 0
        entries: []
        leftWarnings: []
        rightWarnings: []
        truncated: false
        entriesLimit: 200
        generatedAt: '2026-04-05T17:22:03.518Z'

    ComparisonWithDifferences:
      value:
        left:
          type: branch
          id: a1Bg8000000AbCdEAK
          label: MBOM-GDL
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab01EAC
          rootRevisionLabel: rev-1
          asOf: '2026-04-05T17:22:03.518Z'
          sourceTimePoint: '2026-01-15T00:00:00.000Z'
        right:
          type: ebom
          id: a0Ag8000000Ab02EAC
          label: Assy-100
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab02EAC
          rootRevisionLabel: rev-2
          # One clock, twice: a raw eBOM walk has no acceptance pinning it, so its traversal clock
          # IS its T_R — here the release date of rev-2, since no rightAsOf was sent.
          asOf: '2026-04-02T00:00:00.000Z'
          sourceTimePoint: '2026-04-02T00:00:00.000Z'
        identical: false
        summary:
          total: 4
          added: 1
          removed: 1
          differing: 1
          # Bracket-005 matched and agreed. Counted here with `includeUnchanged` off, where no
          # `member-unchanged` entry was emitted for it — which is why `byKind` below reads 0.
          unchanged: 1
          byKind:
            root-version-differs: 1
            member-added: 1
            member-removed: 1
            member-differs: 1
            member-unchanged: 0
        entries:
          - entryId: '0c7e5a3419f8b2d6'
            kind: root-version-differs
            differingFields: []
            message: 'The two sides resolve the top-level assembly at different revisions. (Assy-100)'
            target:
              ciKind: source
              edgeId: null
              edgeKey: null
              label: Assy-100
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab01EAC
                childRevisionLabel: rev-1
                quantity: null
                findNumber: null
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab02EAC
                childRevisionLabel: rev-2
                quantity: null
                findNumber: null

          - entryId: '7b31c0d5e9a24f18'
            kind: member-differs
            differingFields: [quantity, childRevision]
            message: 'This member is on both sides, and its content differs. The listed fields are the ones that disagree. (Motor-002)'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD1EAK
              edgeKey: a0Bg8000000Ab10EAC
              label: Motor-002
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              # Claimed, so the branch has spoken about this edge and it answers to the BRANCH
              # clock from then on — which is what `governingClock: branch` states here.
              provenance: claimed
              governingClock: branch
              governingTimestamp: '2026-04-05T17:22:03.518Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab41EAC
                childRevisionLabel: rev-1
                quantity: 4
                findNumber: 10
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab42EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 10

          - entryId: 'c48d90f2a6e17b53'
            kind: member-removed
            differingFields: []
            message: 'This member is present on the left side only. (Harness-011)'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab12EAC
              label: Harness-011
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab22EAC
                childItemNumber: Harness-011
                childItemRevisionId: a0Ag8000000Ab61EAC
                childRevisionLabel: rev-1
                quantity: 3
                findNumber: 30
            right: null

          - entryId: 'af62b1085d3ce974'
            kind: member-added
            differingFields: []
            message: 'This member is present on the right side only. (Panel-300)'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab13EAC
              label: Panel-300
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left: null
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab23EAC
                childItemNumber: Panel-300
                childItemRevisionId: a0Ag8000000Ab52EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 40
        leftWarnings: []
        rightWarnings: []
        truncated: false
        entriesLimit: 200
        generatedAt: '2026-04-05T17:22:03.518Z'

    ComparisonIncludingUnchanged:
      value:
        left:
          type: branch
          id: a1Bg8000000AbCdEAK
          label: MBOM-GDL
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab01EAC
          rootRevisionLabel: rev-1
          asOf: '2026-04-05T17:22:03.518Z'
          sourceTimePoint: '2026-01-15T00:00:00.000Z'
        right:
          type: ebom
          id: a0Ag8000000Ab02EAC
          label: Assy-100
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab02EAC
          rootRevisionLabel: rev-2
          asOf: '2026-04-02T00:00:00.000Z'
          sourceTimePoint: '2026-04-02T00:00:00.000Z'
        identical: false
        summary:
          # The same comparison as `ComparisonWithDifferences`, with `includeUnchanged=true`.
          # `total` moves 4 → 5 and `byKind['member-unchanged']` moves 0 → 1, because both count
          # entries EMITTED. `added`, `removed`, `differing` and `unchanged` are unmoved, because
          # they count correlation keys — `unchanged` is 1 in both responses, which is the whole
          # point of it.
          total: 5
          added: 1
          removed: 1
          differing: 1
          unchanged: 1
          byKind:
            root-version-differs: 1
            member-added: 1
            member-removed: 1
            member-differs: 1
            member-unchanged: 1
        entries:
          - entryId: '0c7e5a3419f8b2d6'
            kind: root-version-differs
            differingFields: []
            message: 'The two sides resolve the top-level assembly at different revisions. (Assy-100)'
            target:
              ciKind: source
              edgeId: null
              edgeKey: null
              label: Assy-100
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab01EAC
                childRevisionLabel: rev-1
                quantity: null
                findNumber: null
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab02EAC
                childRevisionLabel: rev-2
                quantity: null
                findNumber: null

          - entryId: '7b31c0d5e9a24f18'
            kind: member-differs
            differingFields: [quantity, childRevision]
            message: 'This member is on both sides, and its content differs. The listed fields are the ones that disagree. (Motor-002)'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD1EAK
              edgeKey: a0Bg8000000Ab10EAC
              label: Motor-002
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: claimed
              governingClock: branch
              governingTimestamp: '2026-04-05T17:22:03.518Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab41EAC
                childRevisionLabel: rev-1
                quantity: 4
                findNumber: 10
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab42EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 10

          # The one entry `includeUnchanged` adds. It sorts here, between find number 10 and find
          # number 30, because the order is the tree's — breadcrumb path, then find number — not
          # the order the classifier happened to produce.
          - entryId: '1e9f4a7c2b80d635'
            kind: member-unchanged
            differingFields: []
            message: 'This member is on both sides and is identical. (Bracket-005)'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab11EAC
              label: Bracket-005
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab21EAC
                childItemNumber: Bracket-005
                childItemRevisionId: a0Ag8000000Ab31EAC
                childRevisionLabel: rev-3
                quantity: 2
                findNumber: 20
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab21EAC
                childItemNumber: Bracket-005
                childItemRevisionId: a0Ag8000000Ab31EAC
                childRevisionLabel: rev-3
                quantity: 2
                findNumber: 20

          - entryId: 'c48d90f2a6e17b53'
            kind: member-removed
            differingFields: []
            message: 'This member is present on the left side only. (Harness-011)'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab12EAC
              label: Harness-011
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab22EAC
                childItemNumber: Harness-011
                childItemRevisionId: a0Ag8000000Ab61EAC
                childRevisionLabel: rev-1
                quantity: 3
                findNumber: 30
            right: null

          - entryId: 'af62b1085d3ce974'
            kind: member-added
            differingFields: []
            message: 'This member is present on the right side only. (Panel-300)'
            target:
              ciKind: assembly-edge
              edgeId: null
              edgeKey: a0Bg8000000Ab13EAC
              label: Panel-300
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left: null
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab23EAC
                childItemNumber: Panel-300
                childItemRevisionId: a0Ag8000000Ab52EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 40
        leftWarnings: []
        rightWarnings: []
        truncated: false
        entriesLimit: 200
        generatedAt: '2026-04-05T17:22:03.518Z'

    ComparisonTruncated:
      value:
        left:
          type: branch
          id: a1Bg8000000AbCdEAK
          label: MBOM-GDL
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab01EAC
          rootRevisionLabel: rev-1
          asOf: '2026-04-05T17:22:03.518Z'
          sourceTimePoint: '2026-01-15T00:00:00.000Z'
        right:
          type: ebom
          id: a0Ag8000000Ab02EAC
          label: Assy-100
          rootItemId: a02g8000000Ab01EAC
          rootItemNumber: Assy-100
          rootRevisionId: a0Ag8000000Ab02EAC
          rootRevisionLabel: rev-2
          asOf: '2026-04-02T00:00:00.000Z'
          sourceTimePoint: '2026-04-02T00:00:00.000Z'
        identical: false
        summary:
          # `ComparisonWithDifferences` at `entriesLimit=2`. The summary is BYTE-IDENTICAL to the
          # untruncated response — truncation trims the rows and touches nothing else, which is
          # what makes `truncated: true` actionable: 4 entries exist, 2 came back.
          total: 4
          added: 1
          removed: 1
          differing: 1
          unchanged: 1
          byKind:
            root-version-differs: 1
            member-added: 1
            member-removed: 1
            member-differs: 1
            member-unchanged: 0
        entries:
          - entryId: '0c7e5a3419f8b2d6'
            kind: root-version-differs
            differingFields: []
            message: 'The two sides resolve the top-level assembly at different revisions. (Assy-100)'
            target:
              ciKind: source
              edgeId: null
              edgeKey: null
              label: Assy-100
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-01-15T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab01EAC
                childRevisionLabel: rev-1
                quantity: null
                findNumber: null
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: null
                parentItemNumber: null
                childItemId: a02g8000000Ab01EAC
                childItemNumber: Assy-100
                childItemRevisionId: a0Ag8000000Ab02EAC
                childRevisionLabel: rev-2
                quantity: null
                findNumber: null

          - entryId: '7b31c0d5e9a24f18'
            kind: member-differs
            differingFields: [quantity, childRevision]
            message: 'This member is on both sides, and its content differs. The listed fields are the ones that disagree. (Motor-002)'
            target:
              ciKind: assembly-edge
              edgeId: a1Eg8000000AbD1EAK
              edgeKey: a0Bg8000000Ab10EAC
              label: Motor-002
              path:
                - { ciKind: source, label: Assy-100, edgeId: null, edgeKey: null }
            left:
              provenance: claimed
              governingClock: branch
              governingTimestamp: '2026-04-05T17:22:03.518Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab41EAC
                childRevisionLabel: rev-1
                quantity: 4
                findNumber: 10
            right:
              provenance: inherited
              governingClock: source
              governingTimestamp: '2026-04-02T00:00:00.000Z'
              detail:
                ciKind: assembly-edge
                parentItemId: a02g8000000Ab01EAC
                parentItemNumber: Assy-100
                childItemId: a02g8000000Ab20EAC
                childItemNumber: Motor-002
                childItemRevisionId: a0Ag8000000Ab42EAC
                childRevisionLabel: rev-2
                quantity: 1
                findNumber: 10
        leftWarnings: []
        rightWarnings: []
        truncated: true
        entriesLimit: 2
        generatedAt: '2026-04-05T17:22:03.518Z'
